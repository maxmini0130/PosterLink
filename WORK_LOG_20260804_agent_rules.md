# 2026-08-04 코딩 에이전트 규칙 정비

## 1. 배경

- 공유된 PosterLink 수정 방향을 검토했다.
- 기능 로드맵을 한꺼번에 구현하기 전에 Claude Code와 Codex가 같은 제품·데이터·운영 원칙을 따르도록 저장소 루트 규칙을 먼저 확정하기로 했다.
- 기존 루트에는 `CLAUDE.md`와 `AGENTS.md`가 없었고, `README.md`에는 현재 크롤러, 환경변수, 검증, DB, 배포, 관리자 경로가 충분히 기록되어 있지 않았다.

## 2. 결정

- 제품 목적을 `공공 기회 발견·판단·신청 연결`로 정의한다.
- 실제 주관기관·수집 출처·신청 기관, 원문 URL·신청 URL을 분리한다.
- 미확인 모집기간을 상시모집으로 추정하지 않고 모든 날짜는 `Asia/Seoul` 기준으로 처리한다.
- 실제 포스터와 첨부 포스터를 검증하고, AI 불확실성은 검수 대기로 보낸다.
- DB 변경은 마이그레이션으로 추적하며 운영 DB 쓰기는 명시적 승인 후 실행한다.
- 코드 변경은 범위별 테스트, 문서 변경은 UTF-8·명령·링크·포맷 검증을 완료 기준으로 삼는다.

## 3. 변경

- `CLAUDE.md`: Claude Code용 상세 개발·데이터·운영 규칙 추가
- `AGENTS.md`: Codex를 포함한 코딩 에이전트 공통 규칙 추가
- `README.md`: 현재 구조, 실행, 환경변수, 검증, DB, 주요 경로, 배포 정보를 보완
- 이 작업은 문서만 변경하며 DB, 운영 데이터, 배포 설정은 변경하지 않는다.

## 4. 검증

- [x] 문서 UTF-8 확인
- [x] Prettier 확인
- [x] `git diff --check`
- [x] 문서에 기록한 패키지 필터와 명령 확인
- [x] 커밋 및 원격 반영 확인

## 5. 다음 작업

- 공유 방향의 다음 단계는 현재 데이터 모델과 상세 페이지를 실제 코드·운영 데이터 기준으로 감사하고, 가장 작은 개선 단위부터 순서대로 구현하는 것이다.
- 구현 전 각 단계에서 스키마 변경, 운영 데이터 보정, UI 변경을 분리하고 승인·검증 범위를 명확히 한다.

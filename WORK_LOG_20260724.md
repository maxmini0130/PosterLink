# PosterLink 작업 정리 및 통합 로드맵 - 2026-07-24

## 기준

- `WORK_LOG_20260723.md`와 `WORK_LOG_20260723_config_editor.md`의 전체 항목을
  현재 `main` 코드와 대조했습니다.
- 과거 로그는 당시 기록으로 보존하고, 완료 여부와 다음 진행 순서는 이 문서를
  최신 기준으로 사용합니다.
- 현재 로컬 `main`은 `origin/main`보다 3개 커밋 앞서 있습니다.

## 이번에 반영한 작업

- 수집 설정 구조화 에디터 후속 작업을 로컬 `main`에 병합했습니다.
  - `config_json` 구조화 편집
  - 고급 JSON 전환
  - 알 수 없는 설정 키 보존
  - 클라이언트·API 양쪽 저장 전 검증
  - 게시판 목록의 안정적인 React key
  - 파싱·직렬화·검증 테스트 13건

- 포스터 품질 기준을 추가했습니다.
  - 직원·계약직·사회복지사·매니저 채용공고 제외
  - 강사 모집과 통장 모집 공고 제외
  - 태극기 달기운동 같은 행정 캠페인 첨부문서 제외
  - 월·요일 표기만 다른 동일 프로그램 중복 병합
  - `신규 프로그램 안내`와 월 표기만 다른 동일 프로그램 중복 병합
  - 관련 회귀 테스트 8건

- 기존 데이터 정리 도구를 추가했습니다.
  - 제외 대상 제목과 알려진 중복 쌍을 dry-run으로 먼저 조회
  - 중복은 공개 상태를 우선하고, 같은 상태면 먼저 생성된 항목을 대표로 유지
  - 적용 모드에서는 포스터 레코드와 연결된 Storage 이미지를 함께 정리
  - 현재 로컬에는 service-role 키가 없어 검수 대기 데이터의 실제 삭제는 보류

## 2026-07-23 로그 항목별 현재 상태

### 완료

- 수집 기관 운영 화면, 단일·다중 실행 요청, 실행 이력 및 진단
- 지연 실행 표시·알림·관리자 종료 처리
- 기관 포커스 링크와 수집 실행 요약
- 구조화 수집 설정 에디터와 저장 전 검증
- 첨부파일 분석, 원문 추적, OCR 인사이트 기반
- 이미지 없는 공고 후보 분리와 관리자 관리 화면
- 후보 선택 일괄 상태 변경·삭제
- 중복 의심 필터와 후보 비교 화면
- 후보에서 포스터 검수 항목으로 전환
- 비용 없는 Canvas 템플릿 포스터 생성
- 방문 통계 기반과 관리자 통계 화면
- Google/Naver 소유권 확인 파일과 사이트맵
- 메인 모바일 가독성 조정
- 포스터 제외 및 중복 판정의 1차 강화

### 부분 완료

- 기관 확장
  - 마포 지역과 일부 중앙기관 seed·어댑터는 갖춰졌습니다.
  - 실제 운영 수집의 유효율을 기준으로 기관별 활성화·중단 판단이 더 필요합니다.

- 기관별 수집 규칙 튜닝
  - 진단·제외 샘플·원문 추적 근거는 표시됩니다.
  - 실제 운영 데이터로 셀렉터와 필터를 반복 조정해야 합니다.

- PDF/HWP/HWPX 분석
  - PDF, HWPX, DOCX 분석 기반과 외부 HWP 추출 명령 연결점이 있습니다.
  - 구형 HWP 및 필드 구조화 정확도와 실패 사유 표시는 계속 개선해야 합니다.

- OCR 구조화
  - 이미지 OCR 요약과 검증 근거는 저장됩니다.
  - 대상·기간·장소·신청방법·문의처를 안정적인 개별 필드로 만드는 작업은 남았습니다.

- 중복 판정·병합
  - 원문 URL, 이미지, 기관명, 제목 유사도, 마감일을 사용합니다.
  - 첨부파일 해시, 신청 URL, 본문 임베딩과 출처 병합의 완전한 운영 흐름은 남았습니다.

- AI 포스터 생성
  - 현재는 브라우저 Canvas 기반 수동 템플릿 생성입니다.
  - 실제 생성형 AI 호출, 비용 한도, 실행 이력, 프롬프트 버전 관리는 아직 없습니다.

- 사용자 제보
  - 댓글 신고와 관리자 신고 화면은 존재합니다.
  - 공고 URL, 누락 기관, 포스터 이미지, 마감·오류·중복 제보를 하나로 받는 흐름은 남았습니다.

### 미착수 또는 운영 확인 필요

- 로그인한 관리자 세션에서 구조화 에디터의 실제 저장·재조회 확인
- 운영 환경에서 수집부터 승인·메인 노출까지 전체 시나리오 재검증
- 기존 검수 대기 데이터 정리
- 기관 담당자 인증·직접 등록·수정·성과 페이지
- 반복 실패·구조 변경·유효율 급락·필수정보 누락률의 자동 감지
- iOS 네이티브 앱

## 다음 실행 순서

### 1. 배포 가능한 기준선 확정

- 로컬 `main`의 3개 커밋을 원격에 반영합니다.
- 배포 후 `/admin/collection-sources`, `/admin/notice-candidates`,
  `/admin/posters`, `/admin/notifications`를 관리자 계정으로 확인합니다.
- 구조화 에디터에서 실제 기관 설정을 열고 저장한 뒤 값이 손실되지 않는지 확인합니다.

### 2. 기존 데이터와 수집 품질 정리

- 로컬 환경에 `SUPABASE_SERVICE_ROLE_KEY`를 안전하게 설정합니다.
- `scripts/crawler/src/cleanup-feedback-posters.js`를 dry-run으로 재실행합니다.
- 삭제 대상과 대표 중복 항목을 확인한 뒤 `--apply`로 정리합니다.
- 유효 공고 비율이 높은 기관 5~10개를 실행해 신규 제외 규칙과 중복 병합 결과를 확인합니다.

### 3. 운영 반복 작업 단축

- 구조화 에디터의 adapter 자유 입력을 실제 어댑터 목록 기반 선택 UI로 개선합니다.
- 후보 일괄 작업을 다건 API로 바꿔 현재의 후보별 반복 요청을 줄입니다.
- 품질 규칙에 의해 제외된 제목·기관·사유를 관리자 화면에서 쉽게 재검토하게 합니다.

### 4. 데이터 품질 고도화

- 첨부파일 해시와 신청 URL을 중복 지문에 추가합니다.
- 병합 시 대표 포스터에 보조 출처와 링크를 보존합니다.
- OCR 결과를 기간·대상·장소·신청방법·문의처 필드로 구조화합니다.
- PDF/HWP/HWPX 실패 유형을 표준화해 기관 진단에 집계합니다.

### 5. 신규 기능

- 공고 URL·이미지·누락 기관·마감 오류·중복 신고를 받는 통합 제보 흐름
- 기관 담당자 인증, 공고 등록·수정·마감·성과 확인
- 관리자 수동 AI 포스터 생성
  - 호출 전 예상 비용과 월 한도
  - 생성 상태와 실패 사유
  - 프롬프트 버전과 입력 데이터
  - 생성 이미지와 최종 승인 이력

### 6. 모바일

- 우선 PWA와 모바일 웹의 핵심 사용자·관리자 흐름을 안정화합니다.
- 사용 지표와 운영 필요가 확인된 뒤 iOS 네이티브 앱 범위를 확정합니다.

## 검증 결과

- 수집 설정 스키마 테스트: 13 passed
- 포스터 품질·중복 규칙 테스트: 8 passed
- 웹 TypeScript 검사: 통과
- `git diff --check`: 통과

## 현재 차단 요소

- 검수 대기 데이터 조회·삭제에는 Supabase service-role 키가 필요합니다.
- 로그인 관리자 UI 검증에는 관리자 세션 또는 E2E 관리자 계정 환경변수가 필요합니다.
- 원격 반영과 배포는 별도 실행이 필요합니다.

## 2026-07-24 운영 검증 및 데이터 정리 추가 진행

- 운영 관리자 E2E 환경변수와 `e2e.admin@posterlink.kr` 계정을 연결했습니다.
- 운영 사이트 관리자 E2E 13개 시나리오가 모두 통과했습니다.
- 사용자 피드백 대상 기존 데이터에서 비포스터 14건과 중복 2건을 삭제하고 연결된 Storage 파일을 함께 정리했습니다.
- 중복 검토가 기존 `reject` 판정을 `review`로 되돌리던 오류를 수정했습니다.
- 채용공고 재수집 검증 결과, 삭제한 마포구고용복지지원센터 채용글은 다시 생성되지 않았습니다.
- 상세페이지 공통 메뉴 문구가 정상 문화행사를 일정표로 오인하던 규칙을 제목 전용으로 제한했습니다.
- 결과 발표, 본선 진출자 공지, 행사 취소·변경, 모집 심사 일정 공고는 별도 제목 규칙으로 제외합니다.
- 마포문화원 재수집 결과는 20건 확인, 정상 후보 7건 수집, 제외 13건으로 개선됐습니다.
- 마포문화재단의 `단원 추가모집 심사 일정 공고`는 기존 데이터에서 삭제했고 재수집에서도 제외됨을 확인했습니다.
- 크롤러 회귀 테스트는 17개 모두 통과했습니다.
- 구조화 수집 설정 편집기의 adapter 자유입력을 실제 등록 어댑터 선택 목록으로 변경했습니다.
- 기존 사용자 지정 adapter 값은 그대로 보존하며, 직접 수정은 고급(JSON) 모드에서 계속 지원합니다.
- 반려 포스터 일괄 삭제를 단건 DELETE 반복 호출에서 최대 100건 배치 API 한 번으로 변경했습니다.
- 배치 API가 관리자 권한, 전체 대상 존재 여부, 반려 상태를 먼저 확인한 뒤 Storage와 DB를 정리하고 관리자 이력을 기록합니다.
- 제외 샘플에 수집 기관 식별자를 저장하고, 관리자 실행 상세에서 제목·기관·규칙·사유를 함께 표시하도록 개선했습니다.
- `youth-seoul` 단독 수집이 기본 25분 제한으로 종료되어 해당 대형 수집원만 45분 제한을 적용했습니다.
- 첨부파일 분석 시 SHA-256을 계산해 검증 메타데이터에 보존하고 중복 지문에 반영했습니다.
- 공식 신청 URL도 신규 후보와 기존 포스터 양쪽에서 중복 지문에 포함하되, 공용 신청 페이지 단독으로 자동 병합되지 않도록 제목·기관 신호와 함께 판정합니다.
- 포스터 이미지 OCR 요약의 줄바꿈을 보존하고 기간·대상·장소·신청방법·문의처를 `readableNotice.facts` 구조화 필드로 저장합니다.
- 표본 점검에서 발견한 완료 프로그램 `[소식] … 운영(날짜)` 2건과 문화원장 모집·과거 연도 행사 후보를 기존 데이터에서 정리했습니다.
- 완료 프로그램 보도자료, 기관장 모집, 모집·신청 문구가 없는 과거 연도 행사 제목을 재수집 단계에서 제외합니다.
- 기존 운영 데이터 재대조에서 결과·합격 발표, 휴관·시간표·대관 당첨, 주민등록·선거 안내 등 비포스터 26건을 추가 삭제하고 Storage 객체도 함께 정리했습니다.
- 통장·협의체 위원 모집, 주간구인정보, 선거 행정 안내, 완료 활동 보도자료를 재수집 제외 규칙에 추가했습니다.
- 과학기술정보통신부 명칭을 웹접근성 마크로 오인하던 규칙을 수정해 정상 AI 교육 모집 공고 2건은 보존했습니다.

### 다음 순서

1. `youth-seoul` 45분 제한 재수집 완료 및 결과 확인
2. 8개 표본 기관 수집 결과 중 남은 오탐·누락 표본 점검
3. 8개 표본 기관 결과의 남은 오탐·누락 표본 점검 및 운영 규칙 미세 조정

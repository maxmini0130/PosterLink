# PosterLink 작업 정리 — 2026-07-27 (홈페이지 성능 + 기준정보 정리)

> `SNS_INGESTION.md` 구현 작업(`WORK_LOG_20260726_sns_ingestion.md`)과는 별개로,
> 사용자가 직접 지적한 실서비스 품질 이슈 2건을 다룬 기록이다.

---

## 1. 홈페이지 로딩 속도 — 원인 2개 찾아서 고침, 배포 완료

"홈페이지가 너무 느린 것 같다"는 지적을 받고, 실제 운영 사이트(`www.posterlink.kr`)의
네트워크 타이밍을 직접 측정해서 원인을 찾았다(추측 아님).

### 원인 1 — API 호출이 서로 상관없는데도 순차적으로 실행됨
`apps/web/app/page.tsx`의 데이터 로딩 로직이 "세션 확인 → 프로필 조회 → 메인 공고
목록 → 마감임박 목록 → 홈 요약 통계"를 전부 `await`로 하나씩 기다리며 실행하고
있었다. 서로 의존관계가 없는 것들(메인 목록/마감임박 목록/요약 통계)까지 순차
실행되면서 대기시간이 누적됐다.

**조치**: 독립적인 3개 작업(메인 공고 조회, 마감임박 조회, 홈 요약 통계)을
`Promise.all`로 동시 실행하도록 재구성. 기존 상태 관리 동작(예: 프로필을 실제로
다시 조회했을 때만 `userProfile`을 갱신하는 것)은 그대로 유지하도록 세심하게
맞췄다.

### 원인 2 — 서버 함수가 미국에서, DB는 서울에서 돌고 있었음 (더 큰 원인)
운영 사이트 응답 헤더(`X-Vercel-Id`)를 확인해보니 `icn1::iad1::...`로 나왔다 —
요청은 서울 엣지로 들어오는데 실제 서버리스 함수는 **iad1(미국 버지니아)**에서
실행되고 있었다. 반면 Supabase 프로젝트는 `ap-northeast-2`(서울)에 있다
(`supabase projects list`로 확인). API 응답이 1KB도 안 되는데 0.7~1.8초씩 걸린
이유가 로그 테이블 크기(209/31/3건, 매우 작음 — 확인 후 제외) 때문이 아니라
**매 DB 호출마다 태평양을 왕복**하고 있었기 때문이었다.

**조치**: `apps/web/vercel.json`에 `"regions": ["icn1"]` 추가해서 함수 실행
리전을 서울로 고정.

### 검증 결과 (배포 후 운영 사이트에서 재측정)
- 함수 리전: `X-Vercel-Id: icn1::icn1::...` 로 변경 확인
- API 응답시간: 0.7~1.8초 → 0.3~0.5초
- 병렬 호출 확인: 서로 다른 API 호출들이 이제 5ms 이내로 거의 동시에 시작됨
- 전체 로딩에서 API 대기시간 합계: 약 4.5초 → 약 2초

**커밋**: `aed2584` — `perf: fix homepage load waterfall and Vercel function region mismatch`
(push 완료, Vercel 자동 배포로 실제 운영에 반영됨 — 배포 완료까지 확인함)

---

## 2. 기준정보(카테고리/지역) 점검

`/admin/settings`(기준정보 관리)를 보고 "카테고리와 지역이 더 잘 분류돼야 한다"는
지적을 받아 실제 DB 데이터를 확인했다.

### 2-1. 카테고리 — 서로 다른 두 체계가 섞여 있었음 (2026-07-27 완료)

확인해보니 **완전히 다른 카테고리 체계 2개**가 동시에 존재했다:
- 기존 `categories` 테이블(8개, 관리자 화면에서 보이고 `posters`/`poster_categories`에
  실제로 쓰이는 것): 지원금/복지·교육/취업·문화/행사·주거/금융·소상공인·육아/가족·
  건강/의료·기타
- `SNS_INGESTION.md` Phase 2에서 만든 SNS 분류기의 카테고리(10개,
  `poster_notice_candidates.category`에만 쓰임): 지원사업·채용·공모전·교육강좌·
  행사모집·입찰·정책안내·보도자료·생활정보·기타

Phase 2 작업 당시 기존 이용자용 카테고리와 맞춰보지 않고 문서 부록을 그대로
따라서 생긴 제 실수였다. **사용자 결정: 10개 체계로 통합.**

- 새 카테고리 9개를 `categories` 테이블에 추가(기타는 기존 것 재사용, 새로 안 만듦).
- **당시엔 기존 게시물 449건의 카테고리 연결은 그대로 두고, 키워드 휴리스틱으로
  재배정하지 않았다.** 같은 옛 카테고리("지원금/복지") 안에도 모집공고·사기주의·
  상담프로그램 등 성격이 다른 글이 섞여 있어서, 라이브 사이트의 카테고리 필터가
  틀리게 나올 위험이 있었기 때문.
- 참고: 홈페이지의 "청년/소상공인/창업/교육..." 칩은 `categories` 테이블과 전혀
  무관한, 프론트엔드에 하드코딩된 **세 번째** 목록이었다(`/posters?q=` 텍스트
  검색 바로가기 용도). 이번 정리 범위 밖으로 확인만 하고 안 건드림.

**✅ 2026-07-27 재분류 완료** (`OPENAI_API_KEY` 해결 직후 실행): 새로 만든
`poster-category-classifier.js`(gpt-5-mini, 실제 제목+요약 읽고 판단, 복수
카테고리 허용)로 발행중(published) 포스터 444건을 전부 재분류. 기존 8개
카테고리 링크를 삭제하고 새 10개 체계로 완전히 교체(추가가 아니라 교체).
- 결과: 213건 카테고리 1개, 223건 2개, 8건 3개 — 전부 정상. 발행 포스터
  기준 옛 8개 카테고리 사용 0건(완전히 새 체계로 전환됨).
- 첫 실행에서 2건이 "동일 카테고리 중복 배열" 버그로 실패(분류기가 같은
  카테고리를 두 번 반환) → 분류기에서 중복 제거하도록 고치고 재실행해서 해결.
- **범위 제한**: 이번엔 `poster_status='published'`만 처리. "마감(closed)"
  상태 포스터 약 1000건은 아직 옛 카테고리 그대로임 — 사용자가 "마감된 공고
  숨기기"를 끄면 이 포스터들도 보이므로, 나중에 별도로 처리 필요(사용자 결정:
  지금은 발행중만, 마감된 건 나중에).

**커밋**: `68bf0ce`(스키마), `341d98b`(재분류 실행) — 둘 다 push 완료, 운영
DB에 전부 반영됨.

### 2-2. 지역 — 데이터가 매우 부실함, 확인 중 더 큰 이슈 발견해서 보류

`regions` 테이블 확인 결과 심각하게 부실했다:
- 시도 17개 중 **9개만** 등록(강원·경남·경북·전남·전북·충남·충북·제주 8개가
  통째로 빠짐)
- 시군구는 **서울 25개 구만** 있고, 이미 등록된 다른 8개 시도(경기·부산·대구·
  인천·광주·대전·울산·세종)조차 시군구가 하나도 없음

단, 지역 **필터링 로직**(`apps/web/lib/regionHelpers.ts`의 `getRegionScopeIds`)은
이미 정확하게 구현돼 있음을 확인했다 — 상위 지역을 선택하면 하위 지역을 자동
포함하고, 하위를 선택하면 상위까지 포함한다. 즉 **데이터만 채우면 코드 수정
없이 전국에서 바로 작동**한다.

**작업 중 발견한 중요 사실**: 대한민국 행정구역이 2026-07-01부로 실제로 크게
개편되는 중이었다(여러 언론사·나무위키·국회입법조사처 보고서로 교차 확인,
그리고 이 프로젝트 DB에 이미 몇 주 전 실제로 크롤링돼 있던 마포구청의 진짜
공고 "전남광주 행정통합에 따른 일부 민원서비스 전국 중단 안내"로 재확인됨):
- 광주광역시 + 전라남도가 "전남광주통합특별시"로 통합(대한민국 최초 통합특별시)
- 인천광역시 중구·동구 폐지 → 제물포구·영종구·서해구·검단구 신설
- 화성시·부천시·강원특별자치도(명칭)·전북특별자치도(명칭)·군위군도 개편 대상으로
  행정안전부 공식 추적 페이지(`business.juso.go.kr/jsi/jsiAreaCode`)에 표시됨

해당 공식 페이지의 법정동코드/행정동코드 다운로드는 **INNORIX**라는 정부기관
전용 다운로드 매니저 설치가 필요해서, 브라우저 자동화나 curl로는 파일을 받을 수
없었다. **사용자 결정: 지역 데이터 작업은 일단 보류.** 안정적인 나머지 지역
(190여개)을 먼저 웹검색으로 검증해서 채우는 방법과, 사용자가 직접 파일을
받아서 전달하는 방법을 제안했으나, 지금은 착수하지 않기로 함.

**커밋 없음** — 조사만 하고 데이터/코드 변경 없음.

---

## 다음에 이어서 할 것

1. `OPENAI_API_KEY` 추가되면: (a) `SNS_INGESTION.md` Phase 2/3의 "소식" 127건
   재분류, (b) 기존 게시물 449건의 카테고리를 새 10개 체계로 정확히 재분류
   (복수 태그 허용).
2. 지역 데이터: 사용자가 재개를 요청하면, 공식 파일을 받아왔는지 먼저 확인하고
   없으면 안정적인 지역부터 웹검색 기반으로 채우는 방식 재논의.
3. `SNS_INGESTION.md` Phase 4 잔여 작업(창조경제혁신센터 16곳 SNS 계정, 지역
   신용보증재단 17곳 실제 게시판 URL 등)은 `WORK_LOG_20260726_sns_ingestion.md`
   참고.

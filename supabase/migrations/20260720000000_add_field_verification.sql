-- ============================================================
-- AI 필드 검증 결과 저장
-- 크롤러가 정규식으로 추출한 마감일/기관명을 AI가 재검증한 결과를 보관
-- ============================================================

ALTER TABLE posters ADD COLUMN IF NOT EXISTS field_verification JSONB;

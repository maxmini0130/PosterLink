# PosterLink 개발 규칙

이 문서는 Claude Code가 PosterLink 저장소에서 작업할 때 지켜야 하는 프로젝트 규칙이다. 제품 배경은 `docs/`, 현재 운영 이력은 루트 `WORK_LOG*.md`, 실제 구현 상태는 코드와 `supabase/migrations/`를 기준으로 판단한다.

## 1. 서비스 목적

- PosterLink는 단순 공고 모음이 아니라 사용자가 공공 기회를 발견하고, 자신에게 맞는지 판단하고, 실제 신청까지 이어가도록 돕는 플랫폼이다.
- 사용자가 원문을 다시 해석하지 않아도 되도록 제목, 주관기관, 모집기간, 대상, 혜택, 신청방법, 문의처, 원문과 신청 링크를 정확하게 구조화한다.
- 정보량보다 신뢰성을 우선한다. 불확실한 값을 그럴듯하게 채우지 말고 근거, 신뢰도, 검수 상태를 남긴다.

## 2. 작업 시작 절차

1. `git fetch origin`, `git status --short --branch`, 현재 브랜치와 `origin` 차이를 확인한다.
2. 루트 `README.md`, 관련 `docs/*.md`, 최신 `WORK_LOG*.md`, 대상 코드와 테스트를 먼저 읽는다.
3. 사용자의 미커밋 변경을 보존한다. 직접 만들지 않은 변경을 되돌리거나 덮어쓰지 않는다.
4. 기존 컴포넌트, API, 스키마, 크롤러 어댑터, 공통 유틸리티를 검색한 뒤 재사용한다.
5. 작업 범위를 정하고 코드, DB, 운영 데이터, 배포 영향 여부를 구분한다.

## 3. 저장소 구조와 도구

- 패키지 관리자는 `pnpm@10.33.0`, 런타임 기준은 Node.js 20이다.
- `apps/web`: Next.js App Router 사용자·운영자·관리자 웹, 개발 포트 4000
- `apps/mobile`: Expo 기반 운영자 촬영·업로드 앱
- `packages/ui`, `packages/types`, `packages/lib`: 공유 코드
- `scripts/crawler`: 현재 운영 크롤러, AI 검증, 데이터 감사·복구 도구
- `supabase/migrations`: 추적 가능한 DB 스키마와 정책 변경의 유일한 위치
- `posterlink-crawler`: 별도 레거시 크롤러이므로 사용처를 확인하기 전 현재 운영 코드로 가정하지 않는다.

## 4. 데이터 품질 원칙

- 실제 주관기관, 게시물을 발견한 수집 출처, 접수·신청 기관을 별도 개념으로 다룬다.
- 원문 상세 URL과 실제 신청 URL을 구분한다. 목록 URL이나 기관 홈을 상세 원문으로 대체하지 않는다.
- 모집기간을 확인하지 못했다는 이유만으로 `상시모집`으로 만들지 않는다. 미확인 상태와 실제 상시모집을 구분한다.
- 날짜와 마감 상태는 `Asia/Seoul` 기준으로 해석한다. 원문의 시각·시간대가 있으면 함께 보존한다.
- 원문 텍스트, 정규화 결과, 추출 근거, 수집 시각을 가능한 한 보존한다. 표시용 요약 때문에 원문 데이터를 파괴하지 않는다.
- 핵심 모집 요약은 의미 단위 문단과 목록 구조를 유지한다. 과도한 들여쓰기, 붙어 버린 줄바꿈, 메뉴·푸터·첨부파일 목록 같은 잡음을 제거한다.
- 기존 레코드는 안정적인 출처 식별자와 원문 URL을 중심으로 중복 판정한다. 제목 유사성만으로 병합하지 않는다.

## 5. 포스터 이미지와 AI 검증

- 대표 이미지는 실제 포스터, 모집 안내문 또는 카드뉴스인지 검증한다. 로고, 배너, 썸네일, 지도, 일반 사진, 빈 이미지, 홈페이지 장식 이미지를 포스터로 채택하지 않는다.
- 본문 이미지뿐 아니라 원문에 `포스터`, `홍보물`, `웹포스터` 등으로 첨부된 파일도 후보로 수집한다. 후보의 해상도, 파일명, 주변 문맥, OCR 내용과 공고 일치도를 함께 평가한다.
- AI 판정은 규칙 기반 선별을 보완한다. 모델 실패나 낮은 신뢰도를 자동 승인으로 바꾸지 않는다.
- 판정 결과에는 가능하면 모델·규칙 버전, 신뢰도, 근거, 원본 후보 URL, 검수 상태를 남긴다.
- 포스터 여부나 필드 정확도가 불확실하면 검수 대기로 보낸다. 비포스터가 사용자 화면에 노출되지 않도록 품질 게이트를 유지한다.
- 사람 골든셋의 확정 라벨과 수정 메모를 우선한다. 기계 예측으로 사람의 확정 판정을 조용히 덮어쓰지 않는다.

## 6. 구현 원칙

- TypeScript 타입과 Zod 등 기존 구조화 검증을 활용한다. `any`, 임시 타입 단언, 문자열 기반 임시 파싱을 최소화한다.
- 기존 API와 컴포넌트를 확장할 수 있으면 중복 엔드포인트나 유사 컴포넌트를 만들지 않는다.
- 목업 데이터를 실제 데이터처럼 노출하지 않는다. 빈 상태, 로딩, 오류, 권한 거부 상태를 구현한다.
- UI 변경은 모바일과 데스크톱에서 확인하고, 키보드 접근, 레이블, 대비, 텍스트 넘침을 점검한다.
- 크롤러 파서는 출처별 차이를 어댑터에 한정하고, 공통 정규화·검증·업로드 로직을 우회하지 않는다.
- 외부 요청에는 명시적 타임아웃과 실패 격리를 둔다. 한 출처의 정체가 전체 일일 수집 결과를 잃게 만들지 않는다.

## 7. DB와 운영 안전

- 스키마, 함수, 인덱스, RLS 변경은 새 `supabase/migrations/*.sql` 파일로 기록한다. 적용된 마이그레이션을 사후 수정하지 않는다.
- 운영 DB 마이그레이션, 대량 데이터 수정, 삭제, 백필의 `--apply` 실행은 해당 작업에 대한 사용자의 명시적 승인 없이 수행하지 않는다.
- DB 변경 작업은 먼저 SQL 또는 스크립트, 예상 영향, 검증 쿼리, 롤백·복구 절차를 준비한다.
- 서비스 역할 키로 RLS를 우회하는 코드를 사용자 요청 처리 경로에 추가하지 않는다. 관리자·운영자 권한은 서버에서 재검증한다.
- 환경변수 값, 서비스 역할 키, 개인 데이터는 로그·문서·커밋에 기록하지 않는다.
- 읽기 전용 감사와 dry-run을 우선하고, 쓰기 작업 전에는 대상 건수와 조건을 출력한다.

## 8. 검증 기준

변경 범위에 맞는 검증을 모두 통과해야 완료로 본다.

- 공통: `git diff --check`, 관련 단위·회귀 테스트
- 웹: `pnpm --filter web lint`, `pnpm --filter web build`
- 루트 회귀 테스트: `pnpm test`
- 크롤러: `pnpm --filter posterlink-crawler test`
- AI·데이터 품질 변경: 관련 dry-run, 감사 스크립트, 필요 시 `pnpm --filter posterlink-crawler ai:healthcheck`
- 모바일: `pnpm --dir apps/mobile typecheck`
- UI 변경: Playwright로 주요 흐름과 모바일·데스크톱 화면 확인
- 문서 전용 변경: UTF-8, 링크·명령 정확성, Prettier, `git diff --check`

검증을 실행할 수 없거나 실패가 남으면 완료라고 표현하지 말고 이유와 영향을 보고한다.

## 9. 문서와 작업 로그

- 코드와 직접 관련된 문서만 함께 갱신한다. 과거 회의록과 완료된 작업 로그를 현재 관점으로 전면 재작성하지 않는다.
- `README.md`는 현재 설치, 환경변수, 실행, 마이그레이션, 테스트, 배포, 관리자 진입 경로를 반영한다.
- 의미 있는 작업은 `WORK_LOG_YYYYMMDD_<topic>.md`에 문제, 결정, 변경, 검증, 남은 작업을 기록한다.
- 작업 목록은 실제 상태에 맞춰 진행 중에 갱신한다. 예정 작업을 완료로 표시하지 않는다.

## 10. Git과 완료 보고

- 커밋은 검증된 관련 변경만 포함하고, unrelated 변경을 섞지 않는다.
- `git reset --hard`, 강제 푸시, 사용자 변경 삭제 같은 파괴적 명령을 사용하지 않는다.
- 배포나 워크플로 실행은 결과가 끝날 때까지 확인한다. 실패하면 로그와 원인을 확인한 뒤 보고한다.
- 최종 보고에는 변경 파일, DB·운영 데이터 변경 여부, 실행한 검증, 사용자 영향, 미완료 항목, 배포 상태와 필요한 롤백 방법을 포함한다.

# 2026-09-23 마감 공고 알림 CTA

## 목표와 범위

- 구글 검색으로 마감 공고 상세에 진입한 사용자를 알림 신청으로 전환한다.
- CTA는 `poster_status = closed`인 공고 상세에만 노출한다.
- 진행 중 공고, 홈, 검색 결과, 기관 상세는 이번 작업에서 변경하지 않는다.
- 운영 DB는 읽기만 한다.
- 각 단계가 끝날 때 결과를 보고하고 다음 단계 승인을 기다린다.

## 전체 ToDo

- [x] 0단계: 최근 공고 빈도, 알림 시스템, 마감 상세 현황 분석
- [x] 1단계: 현재 구현과 운영 데이터를 읽기 전용으로 검증
- [ ] 1단계 보완: 분리된 개발/스테이징에서 실제 알림 생성·발송 E2E 수행
- [x] 승인: 1단계 실패 항목 수정 범위 확정
- [x] 알림 조건을 지역·분야 조합 단위로 저장할 스키마 설계 및 migration 작성
- [x] 신규 매칭의 지역·분야 조건을 확정된 규칙으로 변경
- [x] 마감 알림 D-7 / D-1 지원 및 회차별 중복 방지 추가
- [x] Expo 전송 성공·실패·제외 로그 저장 추가
- [ ] 운영 migration 적용 및 Edge Function 배포 승인
- [ ] 2단계: 마감 공고 상세 CTA, 로그인 안내, 조건 확인·수정, 저장 완료 흐름 구현
- [ ] 3단계: `notification_sent`를 포함한 7개 분석 이벤트와 공통 비식별 속성 구현
- [ ] 4단계: lint, typecheck, production build, E2E, 모바일 화면 검증
- [ ] 관련 문서와 작업 로그 갱신 및 검증된 변경 커밋

## 0단계 결과

### 최근 90일 공고 빈도

요청 SQL의 `items` 테이블은 현재 `public` 스키마에 없다. 저장소 migration에서 대응되는 수집 후보 테이블은 `poster_notice_candidates`다.

- `poster_notice_candidates`: 2,206건, 11개 조합 중 월 3건 미만 1개(9.1%)
- 다만 1,924건이 지역·분야 모두 미지정이어서 CTA 기본값 판단 자료로 부적합하다.
- 실제 상세 페이지 분류 구조인 `posters + poster_regions + poster_categories`: 1,275개 공고, 216개 조합 중 월 3건 미만 169개(78.2%)

권장 기본값:

1. 시군구+분야가 월 3건 이상이면 그대로 사용한다.
2. 부족하면 시도+분야로 넓힌다.
3. 그래도 부족하면 지역 전체+해당 분야 또는 해당 지역+분야 전체 중 최근 빈도가 높은 조건을 사용한다.

### 현재 알림 시스템

- `notify-new-match`: 공고가 `published`로 변경될 때 DB trigger가 `new_match` 알림을 만들고, 관리자 호출 Edge Function이 Expo Push를 보낸다.
- `check-deadlines`: 매일 UTC 00:00(KST 09:00)에 실행되며, KST 기준 다음 날 마감하는 찜 공고의 D-1 알림만 만든다.
- 채널: 서비스 내 알림함과 Expo 앱 푸시. 이메일과 브라우저 웹푸시는 구현되어 있지 않다.
- 조건 저장: 전용 구독 테이블 없이 `profiles.is_notified`, `profiles.primary_region_id`, `user_interest_categories`, `institution_follows`, `favorites`를 조합한다.
- Kakao OAuth scope에는 닉네임과 프로필 이미지만 있고 이메일 요청은 없다.

### 마감 상세 현재 상태

- 종료일 계산 결과로 `마감됨` 상태 배지만 표시한다.
- `poster_status = closed` 전용 안내 또는 알림 CTA는 없다.
- 상세 페이지에 유사·관련 공고 추천은 없다.
- 마감 상태에서도 원문 링크 버튼은 그대로 노출된다.

## 1단계 검증 결과

검증일: 2026-09-23 (Asia/Seoul)

환경 확인:

- Supabase 프로젝트는 `posterlink-dev` 한 개이며 실제 서비스가 같은 프로젝트를 사용한다.
- Supabase database branch는 없다.
- 로컬 환경에는 Docker가 없어 로컬 Supabase 통합 테스트를 실행할 수 없다.
- 운영 Edge Function은 `check-deadlines` v7, `notify-new-match` v5로 ACTIVE다.
- 운영 DB 쓰기와 실제 Expo 발송은 수행하지 않았다.

| 항목                    | 결과      | 근거                                                                                                                                                                              |
| ----------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 유사 공고 신규 등록 1회 | 부분 통과 | 같은 사용자·공고·`new_match`의 `NOT EXISTS` 중복 방지는 있다. 다만 trigger가 `AFTER UPDATE`만 처리하므로 처음부터 `published`로 INSERT되는 경로는 알림을 만들지 않는다.           |
| 구독 조건 일치          | 실패      | 현재는 관심 기관 또는 관심 분야 또는 대표 지역 중 하나만 맞아도 발송 대상이다. 지역·분야 조합 조건을 저장하거나 AND로 검증하는 구조가 없다.                                       |
| 같은 공고 중복 방지     | 통과      | 신규 매칭과 마감 알림 모두 사용자·공고·알림 유형 기준 기존 레코드를 확인한다.                                                                                                     |
| D-7 / D-1 KST           | 실패      | KST 다음 날 범위 계산은 올바르지만 D-1만 구현되어 있고 D-7은 없다. 현재 중복 키로는 같은 유형의 D-7과 D-1을 함께 저장할 수도 없다.                                                |
| 알림 해제 사용자 제외   | 통과      | DB trigger와 두 Edge Function 모두 `is_notified = true`를 확인한다. 관련 단위 테스트도 통과했다.                                                                                  |
| 발송 실패 기록          | 실패      | 일부 DB 오류는 함수 로그에 남지만 Expo HTTP/ticket 실패를 영속 저장하지 않는다. `notify-new-match`는 실패 응답을 조용히 건너뛰며 `check-deadlines`도 실패 상세를 저장하지 않는다. |

추가 읽기 전용 확인:

- 전체 crawler 테스트: 312개 통과, 실패 0개
- 대기 `new_match`: 2,939건, 10명, 385개 공고
- 실제 발송 가능한 대기 행: 118건
- push token 없음: 2,821건
- 대기 `favorite_deadline`: 0건
- 최신 대기 목록에 이미 `closed`인 공고도 포함되어 있어 발송 직전에 상태를 다시 거르는 정책이 필요하다.

## 1단계 실패 수정안

다음 변경은 사용자 승인 후 진행한다.

1. 알림 조건 전용 테이블을 추가해 지역 범위, 분야 범위, 기관 조건, 활성 상태를 구독 단위로 저장한다.
2. `notify_new_match_on_publish()`가 구독 한 건의 지역·분야 조건을 함께 만족할 때만 알림을 만들도록 변경한다. 기관 구독은 별도 조건으로 유지한다.
3. 마감 알림에 `deadline_offset_days` 또는 회차 키를 추가해 D-7과 D-1을 각각 한 번씩 생성한다.
4. 모든 날짜 경계 계산을 `Asia/Seoul` 기준 순수 함수로 분리하고 경계 시각 테스트를 추가한다.
5. 푸시 시도 결과를 별도 delivery log에 저장해 성공, 실패, Expo ticket ID, 오류 코드, 재시도 여부를 기록한다.
6. 발송 직전에 공고가 여전히 `published`인지 다시 확인해 마감된 공고의 오래된 대기 푸시를 제외한다.
7. 분리된 개발/스테이징 Supabase 또는 로컬 Docker 환경에서 테스트 계정과 테스트 Expo token으로 실제 E2E를 수행한다.

## 1단계 보완 구현

사용자 승인 후 다음 변경을 로컬 코드와 migration으로 준비했다.

- `alert_subscriptions`: 한 구독 안에 지역·분야 조합 또는 기관 조건을 저장한다. 사용자별 RLS를 적용했다.
- 신규 매칭: 명시적 구독은 지역과 분야를 함께 만족해야 하며, 상위 지역·상위 분야는 하위 분류 공고를 포함한다.
- 기존 관심 설정: 대표 지역과 관심 분야가 모두 일치할 때만 호환 매칭한다.
- 신규 공고 생성 경로: `published` INSERT, 상태 변경, 기관 변경, 공고 지역·분야 연결 추가 시 재평가한다.
- 중복 방지: 같은 사용자·공고의 `new_match`를 unique index와 `ON CONFLICT DO NOTHING`으로 한 번만 만든다.
- 마감 알림: KST 기준 D-7과 D-1을 각각 생성하며 `deadline_offset_days`로 회차별 중복을 막는다.
- 발송 기록: `notification_delivery_logs`에 성공, 실패, 제외, Expo ticket ID와 오류 정보를 저장한다.
- 마감 공고 제외: Edge Function과 수동 발송 스크립트 모두 발송 직전에 `published` 상태를 확인한다. 제외한 대기 알림에는 사유와 시각을 기록한다.
- 감사·정리 스크립트: 발송 대상에서 이미 폐기된 대기 알림을 제외한다.

검증 결과:

- crawler 단위·회귀 테스트 318개 통과
- 신규 KST 경계, D-7/D-1, Expo 성공·실패, 마감 공고 제외 테스트 통과
- 두 Edge Function `deno check` 통과
- Supabase linked migration dry-run 통과: 적용 예정 migration 1개만 확인
- 운영 `new_match` 21,902건을 읽기 전용으로 확인했고 사용자·공고 중복 조합은 0건이었다.
- `git diff --check` 통과

운영 DB migration, Edge Function 배포, 실제 Expo 발송은 수행하지 않았다. 별도 스테이징 프로젝트와 로컬 Docker가 없어 테스트 계정을 사용한 통합 E2E는 아직 수행할 수 없다.

적용이 승인될 경우 순서는 migration 적용, `check-deadlines` 배포, `notify-new-match` 배포, 읽기 전용 상태 확인 순이다. 문제가 생기면 기존 Edge Function 버전을 재배포하고 신규 trigger를 비활성화한 뒤 원인을 확인한다. 신규 테이블이나 로그 데이터 삭제는 별도 승인 없이 수행하지 않는다.

## 다음 승인 지점

1단계 수정 코드는 준비됐지만 운영에는 적용하지 않았다. 다음 단계에서는 마감 상세 CTA와 구독 저장 흐름을 구현하고, 3단계 이벤트 측정까지 연결해야 한다. 단계 규칙에 따라 2단계 승인 전에는 진행하지 않는다.

# 2026-08-04 공고 신청정보 구조화 1차 구축

## 1. 목적

- 개편 가이드에서 브랜치 운영을 제외하고 실제 서비스 구축에 필요한 1차 항목을 반영한다.
- 포스터 이미지와 긴 요약만 보여주는 구조에서 벗어나 사용자가 신청 가능 여부와 핵심 조건을 빠르게 판단하게 한다.
- 수집 출처, 실제 주관기관, 실제 접수기관을 구분하고 확인되지 않은 값은 추측하지 않는다.
- 날짜가 없다는 이유로 `상시 모집`으로 표시하던 오판을 제거한다.

## 2. 데이터 구조

- 마이그레이션 `20260804000000_add_structured_poster_application_facts.sql`을 추가했다.
- 신규 필드:
  - 실제 주관기관, 실제 접수기관
  - 마감 유형, 행사 기간
  - 신청 대상, 연령, 비용, 혜택, 모집 인원
  - 신청 방법, 준비 서류, 문의처, 행사 장소
  - 최종 검증 시각, 검증 상태, 데이터 신뢰도
- 공식 원문과 공식 신청 URL은 기존 `poster_links`의 `official_notice`, `official_apply`를 정본으로 계속 사용한다.
- 공개 상태인 `poster_status`와 데이터 검증 상태인 `verification_status`를 분리한다.
- 운영 DB 마이그레이션은 2026-08-04 적용 완료했다.

## 3. 수집 및 후보 변환

- 크롤러가 원문 검증 결과와 읽기 쉬운 공고 필드에서 구조화 값을 생성하도록 연결했다.
- 명시적 상시·소진 시 문구 또는 실제 마감일이 있을 때만 마감 유형을 확정한다.
- 수집 사이트명을 실제 접수기관으로 간주하지 않는다.
- 관리자 공고 후보 변환 시 신청 대상, 지원 내용, 신청 방법, 문의처를 보존한다.
- 신청 URL은 `official_apply`, 원문 URL은 `official_notice`로 분리 저장한다.
- 마이그레이션 전 배포되어도 기존 컬럼만으로 재시도하도록 웹·크롤러·후보 변환에 호환 경로를 넣었다.

## 4. 상세 화면

- 제목, 신청 상태, 공식 공고 CTA를 포스터 이미지보다 먼저 표시한다.
- 핵심 신청 조건을 신청 대상, 기간, 지역, 연령, 비용, 혜택, 신청 방법, 준비 서류 순으로 분리했다.
- 실제 주관기관, 원문 출처, 접수기관, 검증 상태를 구분해 표시한다.
- `updated_at`을 최종 확인 시각으로 사용하지 않고 `verified_at`만 사용한다.
- 날짜 판정과 D-day 계산은 `Asia/Seoul` 날짜를 기준으로 통일했다.
- 날짜 없음은 `일정 확인 필요`, 명시적 상시만 `상시 모집`으로 표시한다.
- 캘린더 저장은 실제 `.ics` 다운로드로 동작하고, 공유 버튼은 네이티브 공유 실패 시 링크를 복사한다.

## 5. 안전한 기존 데이터 전환

- `structure:backfill` 명령을 추가했다.
- 기본 동작은 dry-run이며 기존 날짜와 `field_verification` 근거만 사용한다.
- 마이그레이션이 없으면 `--apply`를 거부한다.
- 마이그레이션 전 최초 dry-run 결과:
  - 검사 `415건`, 일부 구조화 가능 `262건`, 실제 수정 `0건`
  - 운영 마이그레이션 미적용 상태 정상 감지
- 고신뢰 표본에서도 대상·혜택·장소에 잘린 날짜나 다음 구획이 붙은 사례를 확인했다.
- 기본 백필은 사용자 노출 문구를 제외하고 아래 필드만 적용하도록 제한했다.
  - 실제 주관기관: 신뢰도 `0.8` 이상이며 검수 이슈가 없는 경우
  - 마감 유형: 저장된 신청 마감일 또는 명시적 상시·소진 시 근거가 있는 경우
  - 검증 상태와 데이터 신뢰도
- 대상·혜택·신청방법·문의·장소는 `--include-user-facing-text`를 명시하지 않으면 적용하지 않는다.
- 운영 적용 결과:
  - 검사 `415건`, 수정 대상 `239건`, 적용 `239건`, 실패 `0건`
  - 실제 주관기관 `64건`, 마감 유형 `147건`, 검수 필요 `102건`, 신뢰도 `212건`
  - 대상·혜택·신청방법·문의·장소 `0건`
  - 적용 후 재실행 dry-run 후보 `0건`
- 보고서: `data/results/structured-poster-backfill.json`(git 제외)

## 6. 검증

- 모노레포 단위 테스트: `29/29` 통과
- 크롤러 단위 테스트: `117/117` 통과
- 웹 lint: 경고·오류 없음
- 웹 production build 및 타입 검사: 성공
- 포스터 목록·상세 E2E: `9/9` 통과
- 데스크톱과 Pixel 7 화면에서 실제 공개 공고를 시각 검수했다.
- 실제 공고에서 날짜 미확인, 공식 CTA, 포스터, 핵심 조건, 상세 내용의 표시와 모바일 줄바꿈을 확인했다.

## 7. 작업 리스트

- [x] 신청 판단용 구조화 DB 마이그레이션 작성
- [x] 공통 TypeScript 타입과 KST 마감 판정 추가
- [x] 크롤러 구조화 필드 생성 및 회귀 테스트
- [x] 관리자 후보 변환 시 공식 신청 링크와 핵심 내용 보존
- [x] 마이그레이션 전후 웹·수집 호환 처리
- [x] 상세 화면 정보 우선순위 및 핵심 신청 조건 개편
- [x] 읽기 전용 기존 데이터 dry-run과 결과 보고서 생성
- [x] 단위 테스트, lint, build, E2E, 데스크톱·모바일 검수
- [x] 운영 DB에 신규 마이그레이션 적용
- [x] 표본 검수 후 안전 필드 239건 제한 백필 및 멱등성 확인
- [x] 잘린 날짜·구획 혼입을 막은 뒤 사용자 노출 문구 별도 백필
- [x] 관리자 화면에서 구조화 필드와 검증 상태를 직접 교정하는 편집 기능
- [x] 목록 검색·필터·정렬을 마감 유형과 검증 상태까지 확장
- [x] 검증 완료 구조화 데이터 기반 SEO, 알림, 캘린더 및 운영 지표 확장

## 8. 운영 적용 순서

1. 운영 DB 백업과 마이그레이션 적용 승인을 받는다.
2. `20260804000000_add_structured_poster_application_facts.sql`을 적용한다.
3. `pnpm --filter posterlink-crawler structure:backfill -- --limit=2000`으로 다시 dry-run한다.
4. 고신뢰·검수필요 표본을 관리자와 확인한다.
5. 승인 후에만 같은 명령에 `--apply`를 붙여 적용한다.
6. 상세 화면과 관리자 검수 화면에서 적용 결과를 재검증한다.

## 9. 운영 적용 기록

- Supabase 원격 마이그레이션 이력에서 로컬·원격 `20260804000000` 일치를 확인했다.
- Docker Desktop 미실행으로 `db dump` 스키마 덤프는 생성하지 못했다.
- 변경이 기존 데이터 수정 없는 추가형 마이그레이션임을 확인하고, 백필 전용 롤백 SQL과 적용 전 JSON 보고서를 `supabase/.temp`에 보관했다.
- 운영 DB 직접 조회 결과가 적용 보고서와 일치했다.
- 실제 `https://www.posterlink.kr` 상세 페이지에서 HTTP `200`, 새 상세 레이아웃, 실제 주관기관, 핵심 신청 조건을 확인했다.
- 운영 페이지 브라우저 콘솔 오류는 없었다.

## 10. 관리자 구조화 교정 화면

- 기존 운영자 포스터 편집 화면을 관리자 검수에서도 재사용하는 구조화 교정 화면으로 확장했다.
- 수집 출처 기관, 실제 주최·주관 기관, 실제 접수 기관을 서로 다른 필드로 편집한다.
- 모집·행사 기간, 마감 유형, 자격·연령, 참가비, 혜택, 모집 인원, 신청 방법, 필요 서류, 문의처, 행사 장소를 직접 교정한다.
- 공식 공고와 공식 신청 URL을 각각 `official_notice`, `official_apply`로 저장하고 다른 참고 링크는 보존한다.
- 날짜 입력은 `Asia/Seoul` 기준으로 시작일 00:00:00, 종료일 23:59:59에 저장한다.
- 연령 범위, 날짜 순서, 데이터 신뢰도 범위를 저장 전에 검증한다.
- 사람 검증 완료 시 `verified_at`을 기록하고, 변경 필드를 `field_verification.humanReviewCorrection`에 누적한다.
- 기존 골든셋 교정 출처는 `sources`에 보존하고 현재 편집 출처를 함께 기록한다.
- 관리자가 저장하면 `admin_actions`에 변경 필드와 검증 상태를 `update` 작업으로 남긴다.
- 관리자 미리보기에서 구조화 신청정보와 검증 상태·신뢰도를 표시하고, 날짜 없음은 더 이상 상시 모집으로 표시하지 않는다.
- 게시 중인 포스터도 관리자 미리보기에서 구조화 교정 화면으로 이동할 수 있다.

## 11. 추가 검증 기록

- 구조화 편집 정규화 테스트를 추가해 전체 단위 테스트 `29/29`을 통과했다.
- 웹 lint는 경고·오류 없이 통과했다.
- 웹 production build와 TypeScript 검사를 통과했다.
- 로컬 개발 서버는 `http://localhost:4001`에서 실행했다.
- 저장소의 E2E 관리자·운영자 계정 정보가 현재 인증 서버와 일치하지 않아 인증 화면의 브라우저 시각 검수는 수행하지 못했다.
- 인증 계정이나 운영 데이터는 검증을 위해 임의로 변경하지 않았다.

## 12. 다음 작업 순서

1. [x] 검증 완료된 구조화 데이터만 SEO·알림·캘린더·운영 지표에 사용하도록 확장한다.
2. [x] 관리자 사람 검증 체크리스트와 비관리자 승인 차단·변경 무효화 규칙을 구축한다.
3. [ ] 관리자가 원문과 포스터를 직접 대조한 첫 공고를 `사람 검증 완료`로 승인한다.
4. [ ] 검증 완료 표본이 생긴 뒤 운영 SEO JSON-LD, 캘린더 파일, 마감 알림 준비 지표를 재확인한다.
5. [x] PDF 3단계 검색 목록 SSR, URL 필터, 지역·분야 랜딩, SEO·사이트맵을 구축한다.
6. [x] PDF 4단계 공개 기관 정본, 기관 페이지, 팔로우와 RLS를 구축한다.

## 13. 사용자 노출 문구 정규화 및 운영 백필

- 번호 구획, 글머리표, 이모지 구획, `첨부파일`, `찾아가기`, 해시태그가 앞 필드에 합쳐지는 공고를 줄 단위로 복원한다.
- 대상·혜택·신청방법·문의·장소를 저장하기 직전에 공통 정규화기를 거치도록 수집, LLM 추출, 구조화 필드 생성 경로를 연결했다.
- `~2026`, `2026. 8. 4.(` 같은 잘린 날짜와 `https://www`, `https://forms` 같은 불완전 URL은 사용자 노출 값에서 제외한다.
- 문의 전화·이메일 뒤에 붙은 일반 참고 URL은 제거하고, 월별 모집명을 잘못 재사용한 신청 선택 문구는 자동 공개하지 않는다.
- `factsLlmMeta.filledByLlm`에 기록된 AI 보완 값은 관리자 검수 제안으로 보존하되 구조화 공개 컬럼과 자동 백필에서는 제외한다.
- 공개 상세 화면은 검수용 `field_verification.readableNotice.facts`를 fallback으로 노출하지 않고 구조화 컬럼만 사용한다.
- 관리자 후보 변환 API에도 같은 정규화와 AI 보완 필드 제외 규칙을 적용해 우회 저장 경로를 차단한다.
- 백필 리포트에 출처 URL, 원 추출 사실, AI 보완 필드 목록을 포함해 후보별 근거를 추적할 수 있게 했다.
- 운영 dry-run 결과:
  - 검사 `415건`, 후보 `10건`, 실제 수정 `0건`
  - 대상 `6건`, 혜택·주요 내용 `4건`, 문의 `6건`, 장소 `6건`, 신청방법 `0건`
- 후보 10건의 원문과 정규화 결과를 전수 대조한 뒤 운영 DB에 반영했다.
  - 적용 `10건`, 총 `28개 필드`, 실패 `0건`
  - 적용 후 같은 조건의 재실행 dry-run 후보 `0건`
- 최종 검증:
  - 크롤러 단위 테스트 `128/128` 통과
  - 모노레포 단위 테스트 `29/29` 통과
  - 웹 lint 경고·오류 없음
  - 웹 production build와 TypeScript 검사 성공

## 14. 관리자 목록 구조화 필터와 정렬

- 포스터 검수 목록에 마감 유형 필터를 추가했다.
  - 마감일 고정, 상시 모집, 소진 시 마감, 모집 예정, 일정 확인 필요
- 데이터 검증 상태 필터를 추가했다.
  - 미검증, 추가 검토 필요, 사람 검증 완료, 데이터 사용 불가
- 기본·등록일·마감일·마감 유형·검증 상태 정렬을 서버 조회에 연결하고 복수 정렬 시 등록일을 보조 기준으로 사용한다.
- `deadline`, `verification`, `sort` URL 파라미터의 허용값만 초기 필터로 복원한다.
- 목록 카드에 신청 기간, 마감 유형, 검증 상태를 분리 표시하고 날짜 없음은 `상시`가 아니라 `일정 확인 필요`로 표시한다.
- 필터·정렬 공통 정의와 URL 값 검증, 정렬 우선순위, 적용 조건 수 계산을 별도 모듈과 단위 테스트로 분리했다.
- 운영 DB 읽기 전용 조합 조회 결과:
  - `published + fixed + unverified + 마감일 빠른순` 총 `34건`
  - 첫 페이지 `24건`의 필터 일치와 날짜 오름차순 정렬 확인
- 관리자 인증 E2E 시나리오를 추가했지만 저장된 E2E 관리자 계정이 현재 인증 서버와 일치하지 않아 실행은 자동 스킵됐다.
- 인증 계정과 운영 사용자 데이터는 검증을 위해 임의로 변경하지 않았다.
- 모노레포 단위 테스트 `32/32`, 웹 lint, production build와 TypeScript 검사를 통과했다.

## 15. 검증 완료 구조화 데이터의 기능 연결

- 공통 신뢰 규칙을 `verification_status = verified`이면서 유효한 `verified_at`이 있는 경우로 통일했다.
- SEO:
  - 미검증 주관기관, 신청 대상, 장소, 모집·행사 날짜는 JSON-LD에 포함하지 않는다.
  - 검증 완료된 행사 시작일이 있을 때만 별도 `Event` 엔터티를 생성한다.
  - 중복으로 메타데이터를 만들던 상세 레이아웃 코드를 제거하고 상세 페이지를 단일 정본으로 사용한다.
- 캘린더:
  - 검증 완료 행사 일정 또는 검증 완료 고정 마감일만 `.ics`로 저장한다.
  - 저장 가능한 검증 일정이 없는 공고에는 캘린더 버튼을 표시하지 않는다.
- 마감 알림:
  - 게시 중, 사람 검증 완료, 검증 시각 존재, 고정 마감인 공고만 조회한다.
  - Edge Function의 날짜 경계를 서버 로컬 시간이 아니라 `Asia/Seoul` 내일 00:00~23:59:59로 계산한다.
  - 같은 사용자와 공고에 이미 생성된 마감 알림이 있으면 재실행 시 중복 생성·발송하지 않는다.
  - Expo가 성공 티켓을 반환한 경우에만 `push_sent_at`을 기록한다.
- 운영 지표:
  - 운영자 성과 리포트와 CSV에 검증 상태, SEO 준비, 캘린더 준비, 마감 알림 준비 상태를 추가했다.
  - 화면에서 검증 완료·검토 대기·기능별 준비 건수를 한 번에 확인한다.
- 운영 DB 읽기 전용 현황:
  - 게시 공고 `362건`
  - 미검증 `305건`, 추가 검토 필요 `57건`, 사람 검증 완료 `0건`, 반려 `0건`
  - 현재 SEO 확장·캘린더·마감 알림에 사용할 수 있는 검증 완료 구조화 데이터 `0건`
  - 검증 완료 데이터가 생길 때까지 해당 기능이 구조화 값을 사용하지 않는 것이 의도한 안전 동작이다.
- 회귀 검증:
  - 공통 신뢰 규칙, 캘린더 생성, SEO JSON-LD 단위 테스트를 추가했다.
  - 모노레포 단위 테스트 `40/40` 통과
  - 웹 lint 경고·오류 없음
  - 웹 production build와 TypeScript 검사 성공
  - Edge Function TypeScript를 esbuild로 구문 변환해 문법 오류가 없음을 확인했다.
  - 로컬 미검증 공개 공고에서 JSON-LD에 미검증 주관기관·마감일이 없고 캘린더 버튼도 노출되지 않음을 확인했다.
  - Chromium 데스크톱 `1440x1000`, 모바일 `412x915`에서 HTTP `200`, 가로 넘침 없음, 실패한 하위 요청 없음을 확인했다.
  - 기존 Playwright 묶음은 공개 테스트만 지정해도 전역 인증 준비가 먼저 실행되어 저장된 E2E 계정 단계에서 중단됐다. 계정을 변경하지 않고 동일 Chromium 직접 검수로 공개 화면을 대체 확인했다.
- 운영 반영:
  - 기능 변경 커밋 `cc636c5`를 `main`에 푸시했다.
  - Supabase `check-deadlines` Edge Function을 공식 Management API로 배포해 `ACTIVE`, 버전 `5`, `verify_jwt=false`를 확인했다.
  - `https://www.posterlink.kr`에 배포된 미검증 샘플에서 구버전 캘린더 버튼이 사라지고 JSON-LD에서 미검증 주관기관·마감일이 제외된 것을 다시 확인했다.

## 16. 사람 검증 승인 절차 강화

- 기존 `human_golden_set_seed_20260728_수정.csv`는 포스터 적합성·이미지·원문 링크를 검토한 자료이며 제목·기관·일정·대상·혜택·신청 절차 전체를 라벨링한 자료가 아님을 다시 확인했다.
- 이 골든셋을 근거로 구조화 공고를 자동 `verified`로 승격하지 않았다.
- 관리자 구조화 교정 화면에 아래 사람 검증 체크리스트를 추가했다.
  - 포스터 이미지와 원문 일치·가독성
  - 제목과 실제 주최·주관·접수 기관
  - 모집 기간·마감 유형·행사 일정
  - 대상·연령·비용·혜택·모집 인원
  - 신청 방법·필요 서류·문의처·행사 장소
  - 공식 원문·신청 링크 직접 확인
- 모든 항목 확인, 검토 결과 메모, 공식 원문 URL, 포스터 이미지가 있어야 관리자 검증 완료 저장이 가능하다.
- 부분 검토는 `humanStructuredVerificationDraft`로 저장하고, 완료 검토는 검토자·검토 시각과 함께 `humanStructuredVerification`에 보존한다.
- 검증 완료 공고의 신뢰 대상 값을 다시 편집하면 기존 체크리스트를 초기화한다.
- 운영자는 사람 검증 완료를 선택할 수 없고, 검증된 공고를 수정하면 `needs_review`로 내리며 무효화 사유를 기록한다.
- DB 마이그레이션 `20260804010000_enforce_human_structured_verification.sql`을 추가했다.
  - 관리자 역할과 완전한 체크리스트 증거가 없는 `verified` 전환을 DB에서 차단한다.
  - 본문·날짜·이미지·출처 값이 변경되면 검증을 무효화한다.
  - 공식 링크·분야·지역·포스터 이미지 관계가 변경되어도 검증을 무효화한다.
- 운영 DB 롤백 트랜잭션 검증:
  - 전체 마이그레이션 문법 적용 후 롤백 성공
  - 비관리자 승인 차단 성공
  - 본문 변경 시 자동 재검토 성공
  - 링크 변경 시 자동 재검토 성공
  - 사전 재검토 전환 시 무효화 감사 기록 생성 성공
- 회귀 검증:
  - 모노레포 단위 테스트 `44/44` 통과
  - 크롤러 단위 테스트 `128/128` 통과
  - 웹 lint 경고·오류 없음
  - 웹 production build와 TypeScript 검사 성공
- 운영 반영:
  - 기능 변경 커밋 `5b3e6b9`를 `main`에 푸시했다.
  - 운영 DB에 마이그레이션 `20260804010000`을 적용하고 `supabase_migrations.schema_migrations` 이력을 등록했다.
  - `posters` 승인 강제 트리거 1개와 링크·분야·지역·이미지 변경 무효화 트리거 4개, 총 5개 설치를 확인했다.
  - Vercel production 배포 완료 상태를 확인했다.
- 구조화 필드 전체를 사람이 대조한 기록은 아직 없으므로 자동 승인 없이 `verified`는 `0건`으로 유지했다.

## 17. PDF 3단계 검색 목록과 SEO

- `/posters`를 서버 페이지와 클라이언트 검색 UI로 분리했다.
  - 서버가 첫 공고 목록과 지역·분야 기준정보를 렌더링해 초기 HTML에 실제 공고 링크가 포함된다.
  - 검색어, 분야, 지역, 정렬, 마감 포함 여부를 URL 파라미터와 동기화한다.
  - 검색어 결과는 중복 색인을 막기 위해 `noindex,follow`로 두고 정식 랜딩페이지를 색인 대상으로 사용한다.
- 공개 랜딩페이지를 추가했다.
  - `/regions/[region]`
  - `/categories/[category]`
  - `/regions/[region]/[category]`
- 랜딩마다 동적 metadata, canonical, Open Graph, `CollectionPage`·`ItemList` JSON-LD를 생성한다.
- 대량 관계 ID를 URL의 `in(...)` 조건으로 보내다 길이 제한에 걸리는 문제를 발견해 DB 함수 `search_public_posters`로 조인을 이동했다.
- sitemap에 실제 공개 공고가 연결된 지역·분야·지역+분야·기관 경로를 추가했다.
- 모집 중 공고는 sitemap에서 일간 변경, 종료 공고는 월간 변경으로 분리했다.
- 포스터 카드에서 날짜가 없다는 이유로 `상시 모집`이라고 표시하던 잔여 경로를 제거했다.
  - 명시적 상시·소진 시·예정만 해당 상태를 표시하고 나머지는 `일정 확인 필요`로 표시한다.
- Google Forms 이미지가 브라우저 교차 출처 정책으로 차단되는 사례는 해당 호스트만 기존 이미지 프록시를 사용하고 필요한 요청 헤더를 제한적으로 적용했다.

## 18. PDF 4단계 기관 데이터와 팔로우

- 마이그레이션:
  - `20260804020000_add_public_institutions_and_follows.sql`
  - `20260804021000_add_public_discovery_rpc.sql`
- 공개 기관 정본 `institutions`와 사용자 관계 `institution_follows`를 추가했다.
- 내부 수집 채널 `collection_sources`는 그대로 유지하고 공개 기관과 `institution_id`로 연결했다.
- 공고의 기관 관계를 섞지 않도록 별도 FK로 분리했다.
  - `organizer_id`: 실제 주최·주관기관
  - `source_institution_id`: 게시·수집 출처 기관
  - `application_institution_id`: 신청 접수기관
- 수집 채널을 초기 기관 디렉터리로 옮길 때 `verified`가 아닌 `source_confirmed`로 등록해 기관 검증을 과장하지 않았다.
- 실제 주최·접수기관 FK는 사람 검증 완료 공고만 자동 연결한다.
- 검증 완료 이후 기관 관계를 직접 바꾸면 `needs_review`로 되돌리고 무효화 근거를 기록한다.
- 기관 공개·관리자 수정·본인 팔로우 조회/추가/삭제를 RLS 정책 5개로 통제한다.
- 공개 화면:
  - `/institutions`: 기관명·지역 검색, 현재 모집·확인 공고 기준 정렬
  - `/institutions/[slug]`: 기관 정보, 공식 홈페이지, 주최 공고와 게시 출처 공고 분리
  - `/institutions/following`: 로그인 사용자의 팔로우 기관
  - 마이페이지에 팔로우 기관 진입 추가
- 운영 적용 직후 현황:
  - 공개 기관 `96개`
  - 기관과 연결된 수집 채널 `96개`
  - 게시·수집 출처 기관이 연결된 공고 `328건`
  - 실제 주최기관 연결 공고 `0건`
  - 기관 팔로우 `0건`
- 실제 주최기관 `0건`은 오류가 아니다. 현재 사람 검증 완료 공고가 `0건`이므로 수집 출처를 주최기관으로 복사하지 않은 의도한 결과다.

## 19. 3·4단계 검증 및 운영 반영

- 두 마이그레이션 모두 운영 DB 전체 트랜잭션 적용 후 롤백 검사를 통과했다.
- 롤백 검사에서 다음을 확인했다.
  - 기관 초기 데이터와 출처 공고 연결
  - 관리자 사람 검증 완료 시 실제 주최·접수기관 정본 자동 생성·연결
  - 기관 관계 직접 변경 시 검증 자동 무효화
  - 지역·분야 공개 검색 함수의 실제 결과 반환
- 운영 DB 적용 후 마이그레이션 dry-run에서 미적용 파일 `0개`를 확인했다.
- 초기 HTML 검사:
  - `/posters` 실제 공고 링크 `12개`
  - `/regions/seoul-mapo` `48개`
  - `/categories/course` `48개`
  - `/regions/seoul-mapo/course` `45개`
  - `/institutions/mapo-gu` 게시 출처 공고 `13개`
- Chromium 검수:
  - 데스크톱 `1440x1000`, 모바일 `412x915`
  - 검색 목록, 지역·분야 랜딩, 기관 목록, 기관 상세 HTTP `200`
  - 전체 화면 가로 넘침 없음
  - 검색 분야 선택 후 URL `category=course` 동기화 확인
  - 콘솔 오류·실패한 하위 요청 `0건`
- 회귀 검사:
  - 모노레포 단위 테스트 `51/51`
  - 크롤러 단위 테스트 `128/128`
  - 웹 lint 경고·오류 없음
  - 웹 production build와 TypeScript 검사 성공

## 20. 다음 작업

1. 관리자가 첫 공고의 포스터·원문·구조화 필드 전체를 대조하고 사람 검증 완료로 승인한다.
2. 해당 공고가 실제 주최기관 페이지에 자동 연결되는지 확인한다.
3. 검증 완료 표본의 JSON-LD, 캘린더, 마감 알림 준비 상태를 운영에서 확인한다.
4. PDF 5단계에서 기관 팔로우를 신규 공고 알림과 개인화 추천 점수에 연결한다.

## 21. 첫 사람 검증 후보 준비와 4단계 출처 연결 보완

- 운영 DB 재확인 결과 사람 검증 완료 공고는 `0건`, 검수 필요 게시 공고는 `57건`이었다.
- 첫 사람 검증 후보로 아래 공고를 선정했다.
  - PosterLink ID: `dd64555d-2de7-428a-8fc2-13e4200df562`
  - 공고명: `을지유니크팩토리 <엔딩라이팅 워크숍> 모집`
  - 공식 원문: 청년몽땅정보통 `sprtInfoId=72280`
  - 신청 마감: `2026-08-10`
- 실제 포스터와 공식 원문을 비교해 다음 필드를 검수 가능한 상태로 보정했다.
  - 게시 출처, 실제 주최·접수기관
  - 신청 기간과 고정 마감
  - 2회차 행사 시작·종료 일시
  - 대상 연령, 비용, 모집 인원, 신청 방법
  - 문의처, 장소, 짧은 요약과 줄 단위 상세 요약
- 사람 검증을 가장하지 않도록 상태는 `needs_review`, `verified_at`은 `NULL`로 유지했다.
- AI 준비 내용은 `field_verification.aiStructuredPreparation`에 공식 원문, 변경 필드, 제거한 이미지·링크와 함께 기록했다.
- 해당 공고에 남아 있던 Google Forms 공통 헤더 화면 1장을 포스터 이미지 관계에서 제거했다.
- Google Forms 신고 링크와 제품 소개 링크를 신청 링크에서 제거했다.
- 최종 관계는 실제 포스터 1장, 공식 원문 1개, 공식 신청 링크 1개만 남겼다.

### 재발 방지

- `source_org_name`은 실제 주최기관으로 덮어쓰지 않고 수집 사이트명을 우선 보존한다.
- 실제 주최기관은 `organizer_name`에 별도로 저장한다.
- 담당기관명과 게시 출처가 다른 경우 `source_key`의 URL 경로와 호스트를 `collection_sources`에 대조해 `source_institution_id`를 결정한다.
- 이름 일치, 상세·목록 URL 디렉터리 일치, 단일 호스트 일치 순으로만 연결하고 애매한 호스트는 추정하지 않는다.
- Google Forms가 외부 원문 링크로 발견되면 신청 링크로만 저장한다.
- 신청 폼의 본문, 제목, 첨부, 헤더 이미지는 공식 공고 본문이나 포스터 이미지 후보로 합치지 않는다.
- Google Forms의 `/abuse`와 `/forms/about` 내부 링크를 신청 링크로 분류하지 않는다.
- 마이그레이션:
  - `20260804022000_fix_source_institution_resolution.sql`
  - `20260804023000_prepare_first_human_verified_poster.sql`

### 운영 반영 결과

- 게시 공고 `362건` 중 원문 출처를 식별할 수 있는 `361건`의 `source_institution_id`가 연결됐다.
- 남은 1건은 `source_key`가 없는 수동 미검증 공고이므로 자동 추정하지 않았다.
- 후보 공고의 출처 기관은 `청년몽땅정보통(source_confirmed)`으로 연결됐다.
- 실제 주최·접수기관 FK는 사람 검증 승인 전이므로 계속 `NULL`이다.
- 사람 검증 완료 공고는 계속 `0건`이다.

### 검증

- 크롤러 단위 테스트 `130/130` 통과
- 모노레포 단위 테스트 `51/51` 통과
- 웹 lint 경고·오류 없음
- 웹 production build와 TypeScript 검사 성공
- 두 신규 마이그레이션의 로컬·운영 이력 일치 확인

## 22. 이어서 할 작업

1. 관리자가 준비된 후보의 포스터·원문·구조화 체크리스트를 직접 확인하고 사람 검증 완료로 저장한다.
2. 승인 직후 실제 주최기관과 신청 접수기관 정본이 생성·연결되는지 확인한다.
3. 해당 표본의 Event JSON-LD, 캘린더 저장, 고정 마감 알림 준비 상태를 운영에서 검증한다.
4. 검증 표본이 정상 작동하면 PDF 5단계 기관 팔로우 신규 공고 알림과 개인화 추천을 시작한다.

## 23. 서울특별시 분야별 새소식 수집 추가

- 요청 주소 `https://www.seoul.go.kr/realmnews/in/list.do`를 별도 기관으로 중복 생성하지 않고 기존 `서울특별시(seoul-city)` 수집 채널의 두 번째 게시판으로 등록했다.
- 기존 서울시 공고/고시 게시판도 실제 목록 구조에 맞게 함께 보정했다.
  - JavaScript 상세 링크를 정식 `nttNo` URL로 변환
  - 목록·제목·본문·포스터 이미지·첨부파일 선택자 범위 제한
  - 분야별 새소식의 `fetchStart` 페이지 이동과 `news.seoul.go.kr/.../archives/...` 상세 URL 지원
  - HTTP 이미지·링크를 HTTPS로 정규화
- 신청 폼 같은 외부 페이지는 신청 링크로만 보존하고 공고 제목·본문·이미지·첨부를 덮어쓰지 않도록 공통 판별 함수를 두 어댑터가 함께 사용하게 했다.
- 서울시 상세 HTML에 이미 원문이 있으므로 PDF/HWP 본문 반복 분석은 끄되, 이미지 첨부를 포스터 후보로 올리는 처리는 유지했다.
- 본문 일정의 `발표`·`합격자 발표` 때문에 현재 모집 공고가 결과 공고로 오인되지 않도록 제목 중심 결과 판정을 보강했다.
- 서울시 이미지 없는 텍스트 공고는 제목에 모집·신청·접수·공모 등 행동이 있고 본문에 기간·방법·대상·마감 근거가 있을 때만 허용한다.
  - 행정처분, 공시송달, 무연고, 등록말소, 과태료, CCTV, 도시관리계획 등 일반 행정문서는 제외한다.
- 날짜 파서를 보강했다.
  - `’26. 8. 3.(월) ~ 23.(일)` 같은 축약 연도·월 범위 지원
  - 시작·종료 시각이 포함된 접수기간 지원
  - `2026년 12월 31일까지` 같은 한글 마감 표기 지원
  - 종료일 없는 선착순 신청 뒤의 행사기간을 신청 마감으로 오인하지 않음

### 운영 반영 및 실수집 검증

- 마이그레이션:
  - `20260804024000_add_seoul_realmnews_board.sql`
  - `20260804025000_limit_seoul_attachment_analysis.sql`
- 운영 DB에서 기존 서울특별시 기관 ID `7e6f0b9d-4dd8-4e57-a848-b0ed086d398f` 재사용과 두 게시판 활성화를 확인했다.
- 실목록 2페이지씩 총 40건을 상세 검사해 모집·신청성이 확인된 9건만 로컬 결과로 남겼다.
- 대학생 순찰대 모집의 결과 공고 오탐이 해소됐고 행정처분·공시송달·정책기사 등은 제외됐다.
- 실제 본문 재검증 결과:
  - 자치경찰 시민대학 마감 `2026-08-21`
  - 대학생 순찰대 마감 `2026-08-23`
  - 물산업 R&D 지원사업 마감 `2026-08-31`
  - 에너지바우처 신청 마감 `2026-12-31`
  - 서울농장 3건은 종료일이 없는 선착순 접수이므로 마감을 추정하지 않음
- 로컬 실행 환경에는 `OPENAI_API_KEY`가 없어 포스터·원문 AI 일치 검증이 필요한 이미지는 기본 거절됐다. AI 검증을 우회하지 않기 위해 로컬 결과 9건은 운영 공고로 업로드하지 않았다.
- 커밋 `c9fc51d` 기준 GitHub Actions 운영 크롤러를 `seoul-city` 단독으로 실행했다.
  - 크롤러 회귀 테스트, 운영 비밀키 검사, AI 수집, Supabase 업로드, 결과 아티팩트 생성 성공
  - 후속 AI 품질 게이트는 Node 20에서 Supabase Realtime WebSocket transport가 없어 실패
  - KPI 측정기와 품질 게이트 자체의 두 Supabase 클라이언트에 기존 `ws` transport를 명시해 Node 20 실행 환경을 보정
- 운영 업로드 12건을 포스터 목적과 신청 구조 기준으로 재감사했다.
  - 무연고 사망자, 감염병 예방수칙, 시민투표, 상업 할인·브랜드 기사 5건은 `rejected`로 전환해 검수대기에서 제외
  - 서울시 광역 뉴스는 모집·신청·접수 근거가 없으면 이미지 AI 검사 전에 제외하도록 재발 방지
  - 남은 검수대기 7건은 모두 기존 서울특별시 기관과 연결되고 포스터 이미지가 1장 이상 저장됨
  - 에너지바우처 신청 마감은 `2026-12-31`로 보정
  - 종료일이 명시되지 않은 서울농장 2건은 `deadline_type=unknown`, 마감 `NULL`로 보정
- Node 20에서 KPI 측정과 강제 품질 게이트를 재실행해 통과했다.
  - 임베딩 커버리지 `100%`
  - 필드 검증 커버리지 `61.6%`
  - 이미지 AI 커버리지 `30.9%`
  - 품질 게이트 위반 `0건`

### 검증

- 서울시 전용 회귀 테스트 `5/5` 통과
- 크롤러 전체 단위 테스트 `138/138` 통과
- 모노레포 단위 테스트 `51/51` 통과
- 웹 lint 경고·오류 없음
- 웹 production build와 TypeScript 검사 성공

## 24. PDF 5단계 기관 팔로우 알림과 추천 연결

- 기관 팔로우를 기존 `new_match` 알림 파이프라인에 연결했다.
  - 공고가 새로 `published`될 때 팔로우한 기관이 `source_institution_id`, `organizer_id`, `application_institution_id` 중 하나와 일치하면 알림을 생성한다.
  - 실제 주최기관, 게시 출처, 신청 접수기관 FK가 모두 비어 있으면 추정 알림을 만들지 않는다.
  - 관심 분야·지역 매칭과 기관 팔로우 매칭이 동시에 발생해도 사용자당 공고 1건의 알림만 남긴다.
- `get_recommended_posters` RPC에 팔로우 기관 가중치 `45점`을 추가했다.
  - 기존 지역 `50점`, 관심 분야 `30점`, 임박 마감 `20점` 규칙은 유지한다.
  - 수집 출처 기관과 실제 주최·접수기관을 섞어 저장하지 않고 확인된 FK만 점수에 사용한다.
- `notify-new-match` Edge Function이 DB에 저장된 알림 제목과 본문을 그대로 푸시로 발송하도록 수정했다.
  - 팔로우 기관 알림은 모바일 푸시에서도 `팔로우 기관 새 공고`로 구분된다.

### 검증

- `git diff --check` 통과
- 모노레포 단위 테스트 `53/53` 통과
- `pnpm --filter web lint` 경고·오류 없음
- `pnpm --filter web build`와 TypeScript 검사 성공
- `notify-new-match` Edge Function을 esbuild로 구문 변환해 문법 오류가 없음을 확인
- 운영 DB 마이그레이션 적용과 Edge Function 배포는 2026-08-21에 완료했다.

## 25. 기관 팔로우 기반 알림·추천 운영 반영

- 운영 Supabase 프로젝트 `zxndgzsfrgwahwsdbjdj`에 마이그레이션 `20260804027000_connect_institution_follows_to_alerts_and_recommendations.sql`을 적용했다.
- `notify-new-match` Edge Function을 재배포했다.
  - 배포 전 원격 함수 버전: `3`
  - 배포 후 원격 함수 버전: `4`
- 원격 마이그레이션 목록에서 `20260804027000`의 local·remote 적용 상태가 일치함을 확인했다.
- 배포 후 `notify-new-match` 함수가 `ACTIVE`, `verify_jwt=true` 상태임을 확인했다.
- 배포 중 `supabase/config.toml`의 `[inbucket]` deprecated 경고와 Docker 미실행 경고가 표시되었으나, 원격 함수 업로드와 배포는 정상 완료되었다.

### 검증

- `pnpm test` 53/53 통과
- `pnpm --filter web lint` 경고·오류 없음
- `pnpm --filter web build` 성공
- `pnpm exec esbuild supabase/functions/notify-new-match/index.ts --bundle --platform=neutral --format=esm --outfile=NUL` 성공
- `pnpm dlx supabase migration list --linked`에서 `20260804027000` 원격 적용 확인
- `pnpm dlx supabase functions list --project-ref zxndgzsfrgwahwsdbjdj`에서 `notify-new-match` 버전 `4` 확인

## 26. 홈 추천 v2 기관 팔로우 점수 연결

- 운영 스모크 점검에서 홈 화면이 `get_recommended_posters_v2`를 호출하고 있음을 확인했다.
- 기존 `get_recommended_posters` RPC에는 기관 팔로우 가중치 `45점`이 반영됐지만, 실제 홈 추천 경로인 `get_recommended_posters_v2`에는 아직 반영되지 않은 상태였다.
- `get_recommended_posters_v2`에도 사용자가 팔로우한 기관이 공고의 `source_institution_id`, `organizer_id`, `application_institution_id` 중 하나와 일치하면 `45점`을 더하도록 마이그레이션을 추가했다.
- 기존 v2 추천 신호는 유지했다.
  - 지역 `40점`
  - 관심 분야 `30점`
  - 대상·성별 적합도 `+20점` 또는 `-15점`
  - 7일 이내 마감 `10점`
  - 찜·조회 이력 기반 의미 유사도 최대 `30점`
  - `similarity_score` 반환 컬럼 유지
- 운영 DB 읽기 전용 점검 결과, 2026-08-21 기준 `institution_follows`는 아직 `0건`이므로 실제 기관 팔로우 추천·알림은 사용자가 팔로우를 시작한 뒤 발생한다.
- 운영 DB 적용은 2026-08-21에 완료했다. `supabase db push --dry-run`에서 적용 후보가 새 마이그레이션 1개뿐임을 확인한 뒤 적용했다.

### 검증

- `git diff --check` 통과
- `pnpm test` 53/53 통과
- `pnpm --filter web lint` 경고·오류 없음
- `pnpm --filter web build` 성공
- `pnpm dlx supabase db push --linked --dry-run`에서 `20260821000000_add_institution_follow_score_to_semantic_recommendations.sql`만 적용 후보로 확인
- `pnpm dlx supabase db push --linked --yes`로 운영 DB에 `20260821000000` 적용 완료
- `pnpm dlx supabase migration list --linked`에서 `20260821000000` 원격 적용 확인
- `information_schema.routines`에서 `get_recommended_posters_v2` 함수 존재 확인

## 27. 행정 공고문 스캔 이미지 비포스터 처리

- 검수대기 공고 `공중화장실 관리인 모집 공고`의 대표 이미지를 직접 확인했다.
- 이미지는 제목, 접수기간, 근무조건, 문의처가 있는 실제 공고문이지만, 디자인된 모집 포스터가 아니라 2쪽짜리 행정 공고문 스캔 이미지였다.
- PosterLink 대표 포스터 기준에는 맞지 않으므로 운영 DB에서 해당 공고를 `rejected`로 전환했다.
  - PosterLink ID: `9262ae47-14a1-4837-8261-724a86c140b9`
  - 기존 상태: `review`
  - 변경 상태: `rejected`
  - `field_verification.posterImageOcr.imageClassification.isPoster=false`
  - `visualType=administrative document scan`
- 재발 방지를 위해 포스터 이미지 분류 프롬프트에 다음 기준을 추가했다.
  - 공식 행정문서 스캔
  - 2단/문서형 공고문 이미지
  - 관보·고시형 공고
  - 평문 채용·모집 공고문 이미지
- 품질 게이트의 비포스터 행정 모집 규칙에도 `관리인/관리원/청소원/환경미화원 모집 공고`를 추가했다.

### 검증

- `git diff --check` 통과
- `pnpm --filter posterlink-crawler test` 139/139 통과
- 운영 DB 업데이트 결과 해당 공고가 `rejected`이고 이미지 분류가 `human-review`, `isPoster=false`임을 확인

## 28. 모집시까지 접수기간의 행사일 마감 오인 방지

- `posters.poster_status='review'` 큐는 비어 있음을 확인했다.
- `poster_notice_candidates`의 최근 `pending` 후보를 점검하며, 이미지 없는 텍스트 공고 중 `모집시까지` 같은 열린 접수기간 뒤에 행사일이 이어지는 경우가 반복적으로 `deadline-mismatch`를 만드는 것을 확인했다.
- 날짜 품질 검사에서 `신청기간/모집기간: YYYY. M. D. ~ 모집시까지`처럼 종료일이 없는 열린 접수기간은 `까지` 키워드나 뒤따르는 행사일을 마감일로 승격하지 않도록 보강했다.
- 대표 회귀 케이스를 추가했다.
  - 입력: `모집기간 2026년 8월 20일(목) ~ 모집시까지 진행일시 [1차시] 2026년 9월 4일(금) ...`
  - 기대: `suggestedDeadline=null`, `open-ended-application-period` 이슈 유지, `deadline-mismatch` 미발생
- 기존 운영 후보의 저장된 마감일 일괄 보정은 수행하지 않았다. 필요 시 후보별 원문 확인 후 별도 승인 절차로 처리한다.

### 검증

- `git diff --check` 통과
- `pnpm --filter posterlink-crawler test` 140/140 통과

## 29. 모집시까지 기존 후보 마감일 운영 보정

- `poster_notice_candidates`에서 `모집시까지` 열린 접수기간인데 행사일·선정일이 `application_end_at`으로 저장되고 `deadline-mismatch`가 남아 있던 운영 후보 4건을 보정했다.
- 보정 대상:
  - `7b6161e7-4c0d-4ccd-8d49-0fe7daad57d3` 청년공간 동선이음 `<9월 요리아카데미(1인가구지원):홍소육+토달볶 만들기>` 참여자 모집
  - `87da0301-ad71-4d5d-b699-c5ebada34d23` 청년공간 동선이음 `<9월 요리아카데미(1인가구지원):꽃게탕+무생채 만들기>` 참여자 모집
  - `65695890-a648-456c-8fdf-b61e5b485860` 청년공간 동선이음 `<9월 문화생활:홈베이킹:휘낭시에 3종 만들기>` 참여자 모집
  - `fe1f9c2b-2087-4622-875c-abb32db416f8` 청년공간 동선이음 `<9월 직업지원: 실전대비! 모의면접>` 참여자 모집
- 각 후보의 `application_end_at`을 `NULL`로 되돌리고 `deadline_type`도 `NULL`로 정리했다.
- `quality_issues`에서 잘못된 `deadline-mismatch`를 제거하고 `open-ended-application-period` 검토 이슈를 남겼다.
- 후보 상태는 계속 `pending`으로 유지했으며, 공고 생성·공개·승인은 수행하지 않았다.

### 검증

- 같은 조건의 잔여 `deadline-mismatch` 후보가 `0건`임을 확인
- 보정 4건 모두 `application_end_at=NULL`, `deadline_type=NULL`, `deadline-mismatch=false`, `open-ended-application-period=true` 확인

## 30. 검수 후보 큐 정리와 텍스트 후보 기준 문서화

- 운영 DB 기준 `posters.poster_status='review'` 큐는 비어 있고, `poster_notice_candidates.candidate_status='pending'` 큐는 `1092건`임을 확인했다.
- pending 후보의 주요 반복 이슈를 집계했다.
  - `text-notice-no-image`: 775건
  - `date-without-year`: 254건
  - `weak-summary`: 142건
  - `duplicate-suspected`: 63건
  - `deadline-mismatch`: 32건
  - `employment-recruitment-notice`: 10건
- 명확히 사용자 노출 대상이 아닌 채용·행정 모집 공고 10건과 기존 공개 포스터 중복 1건을 `dismissed`로 정리했다.
  - 마포복지재단 직원·계약직·사회복지사·운전원 채용 공고
  - 마포복지재단 청소년 프로그램 강사 모집 공고와 연장 공고
  - 기존 공개 포스터와 동일한 중복 후보 1건
- 정리 후 pending 큐에서 `employment-recruitment-notice` 이슈가 사라졌고, `duplicate-suspected`는 63건에서 52건으로 줄었다.
- `docs/text_notice_candidate_policy.md`를 추가해 이미지 없는 텍스트 공고 후보의 승인, 보류, 반려 기준을 문서화했다.
  - 원문 상세 URL과 신청 URL 분리
  - 실제 주관기관, 게시 출처, 신청 접수기관 구분
  - 열린 모집기간 처리
  - 중복 의심 후보의 사람 검수 기준
  - 채용·행정 공고문·결과 공지·시설 안내 반려 기준
- `docs/ai_human_golden_set_protocol.md`에서 텍스트 후보 승인 전 해당 기준을 참조하도록 연결했다.

## 31. 기관 팔로우 추천·알림 실제 검증

- 운영 DB에서 실제 `institution_follows`는 여전히 `0건`임을 확인했다.
- 실제 데이터를 남기지 않는 스모크 검증으로 팔로우 1건을 임시 생성한 뒤 즉시 삭제했다.
  - 대상 공고 추천 점수: `59.7705392702692` → `104.770539270269`
  - 점수 증가량: `45`
  - 검증 후 `institution_follows`: `0건`
- `get_recommended_posters_v2`가 홈 추천 경로에서 팔로우 기관 가중치 `45점`을 반영함을 운영 DB에서 확인했다.
- `trg_notify_new_match` 트리거가 운영 DB에 존재하고 활성 상태(`tgenabled='O'`)임을 확인했다.
- 원격 `notify-new-match` Edge Function은 `ACTIVE`, `verify_jwt=true`, 버전 `4` 상태임을 확인했다.

### 검증

- 운영 후보 11건 `dismissed` 정리 결과 확인
- 기관 팔로우 추천 점수 `+45` 반영 확인
- 알림 트리거와 Edge Function 배포 상태 확인
- `git diff --check` 통과
- `pnpm test` 53/53 통과

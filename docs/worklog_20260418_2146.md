# PosterLink 작업 로그 — 2026-04-18 21:46

## 작업 요약

이 세션에서 진행된 전체 작업 내역입니다.

---

## 1. PostgREST 스키마 캐시 이슈 우회 (3개 파일)

**문제**: delta migration으로 생성된 junction table(poster_categories, poster_regions 등)이 PostgREST 캐시에 미반영되어 nested JOIN 시 400 오류 발생.

**수정 파일:**
- `apps/web/app/components/CommentSection.tsx` — profiles 별도 조회
- `apps/web/app/posters/[id]/page.tsx` — category/region 별도 조회
- `apps/web/app/posters/page.tsx` — 필터링 별도 쿼리

**커밋:** `6c8e0f1`

---

## 2. Header 알림 dot 버그 수정

**문제**: 미읽음 알림 여부와 무관하게 항상 빨간 dot 표시.

**수정:** `notifications` 테이블 조회 후 `is_read = false` 건이 있을 때만 dot 표시.

**수정 파일:** `apps/web/app/components/Header.tsx`

**커밋:** `6c8e0f1`

---

## 3. OCR Edge Function — OpenAI GPT-4o-mini Vision 연동

**내용:** 시뮬레이션 데이터 → 실제 OpenAI API 호출로 교체.
- GPT-4o-mini Vision으로 포스터 이미지 분석
- title, sourceOrgName, categoryId, appEndAt, summaryShort, officialLink 추출
- OPENAI_API_KEY Supabase Secret 등록 완료
- Edge Function 재배포 완료

**수정 파일:** `supabase/functions/process-ocr/index.ts`

**커밋:** `d9ec596`

---

## 4. get_recommended_posters RPC 수정

**문제:** RPC 반환 컬럼에 thumbnail_url 누락, poster_status ENUM → TEXT 캐스트 오류로 400 에러.

**수정:** Supabase SQL Editor에서 직접 함수 재생성 (DROP + CREATE).
- `thumbnail_url TEXT` 컬럼 추가
- `poster_status::TEXT` 캐스트 명시

**수정 파일:** `supabase/migrations/20260418000000_delta_schema.sql` (동기화)

**커밋:** `490df87`

---

## 5. Operator 포스터 목록 카테고리/지역 표시 수정

**문제:** nested JOIN으로 카테고리/지역명 미표시.

**수정:** 별도 쿼리 방식으로 전환 (fetchCategoryRegionNames 헬퍼 사용).

**수정 파일:** `apps/web/app/operator/posters/page.tsx`

**커밋:** `490df87`

---

## 6. Admin 기능 구현

### admin/reports
- nested JOIN → 별도 쿼리 (fetchProfileMap 헬퍼)
- 댓글 숨김/신고 기각 처리 시 handled_by 저장

### admin/settings
- 유저 권한 관리 탭 추가 (닉네임 검색 + role 드롭다운)
- 카테고리/지역 기준정보 관리 (기존)

### notifications
- "전체 읽음" 버튼 핸들러 구현

**커밋:** `2c6cf4e`

---

## 7. 모바일 앱 버그 수정

**수정 내역:**
- `Camera` → `CameraView` + `useCameraPermissions` (expo-camera v14 API)
- `getUser()` → `getSession()` hang 버그 수정
- useEffect 분리로 토큰 저장 무한루프 제거
- `poster_images` 테이블 → `thumbnail_url` 직접 저장
- package.json 버전 SDK 50 호환으로 수정 + expo-camera 추가

**커밋:** `cfdfc95`

---

## 8. lib/dummy.ts 제거

**내용:** 더미 데이터 fallback 제거, 실제 DB 데이터만 사용.

**커밋:** `cfdfc95`

---

## 9. 전체 코드 품질 개선 (리팩토링)

### packages/types/index.ts
- PosterStatus ENUM 실제 DB와 일치 (`review_requested`, `expired`)
- PosterCardData, Category, Region, Notification 타입 추가
- thumbnail_url, expo_push_token 필드 추가

### packages/lib/index.ts
- `getDDay(deadline?)` 공통 함수 추가
- `isDeadlineSoon(deadline?)` 공통 함수 추가

### apps/web/lib/posterHelpers.ts (신규)
- `fetchCategoryRegionNames(posterIds)` — M:N 조회 헬퍼 (4곳 중복 제거)
- `fetchProfileMap(userIds)` — 닉네임 조회 헬퍼 (2곳 중복 제거)

### 버그 수정
- `favorites/page.tsx`: `poster_images` → `thumbnail_url`, `getUser` → `getSession`
- `posters/[id]/page.tsx`: toggleFavorite 에러 처리 (DB 실패 시 state 변경 안 함)
- `mypage/page.tsx`, `mypage/comments/page.tsx`: `getUser` → `getSession`
- `PosterCard.tsx`: shared getDDay 사용, tags nullable 처리, 마감임박 rose 색상
- console.log 제거 (operator/posters/new)
- localStorage JSON.parse try-catch 보호

**커밋:** `33d2075`

---

## 커밋 히스토리 (이번 세션)

```
33d2075 refactor: code quality improvements and common module extraction
cfdfc95 fix: mobile app bugs and remove dummy data
2c6cf4e feat: admin reports/settings UI and notification improvements
490df87 fix: operator poster list category/region display and RPC thumbnail_url
d9ec596 feat: implement real OCR with OpenAI GPT-4o-mini Vision (pending API key)
6c8e0f1 fix: bypass PostgREST schema cache and fix notification dot
```

---

## 남은 작업

- [ ] 소셜 로그인 (Kakao/Google) Provider 설정 (코드 완성, 대시보드 설정만 남음)
- [ ] OCR 실제 이미지 테스트
- [ ] 모바일 실기기 테스트
- [ ] PostgREST 스키마 캐시 reload 확인
- [ ] develop → main PR #1 머지

---

## 참고

- GitHub PR: https://github.com/maxmini0130/PosterLink/pull/1
- Supabase 프로젝트: zxndgzsfrgwahwsdbjdj
- 개발 서버: http://localhost:4000

# PosterLink 작업 정리 — 2026-07-28 (디딤돌 R&D 사업계획서 검토 + AI 기능 3건 구현)

> `SNS_INGESTION.md`/`WORK_LOG_20260726_sns_ingestion.md`, 홈페이지 성능/기준정보
> (`WORK_LOG_20260727_site_quality.md`)와는 별개로, 사용자가 준비 중인 디딤돌 R&D
> 첫걸음 트랙 사업계획서(`posterlink_20260728.pptx`)를 검토하고, 거기서 나온
> "구현해야 할 AI 기능" 목록 중 사용자가 지정한 우선순위(추천순) 3건을 실제로
> 구현한 기록이다.

---

## 1. 사업계획서 검토 (`posterlink_20260728.pptx`, 21장)

로컬 환경에 LibreOffice가 없어 슬라이드 이미지 렌더링은 못 했지만(markitdown으로
전체 텍스트는 정확히 추출), 실제 DB/코드 상태와 대조해서 슬라이드 20의 수치
주장을 검증했다.

**발견한 문제 2가지:**
- **"200+ 주요 공공기관 DB 확보"** — 실제 `collection_sources`는 96개(활성 39,
  planned 57)뿐. 근거로 든 "17개 광역시도 경제진흥원/테크노파크"는 2026-07-27에
  막 등록한 것(전부 planned, 크롤링 0건)이었음 — "확보"가 "실제 수집 중"으로
  오독될 소지.
- **Inbound 파이프라인(CLIP+GPT Vision+LLM)이 이미 완성된 기술인데 "R&D
  개발내용"으로 제시됨** — 첫걸음 트랙(중기부 R&D 수행이력 없는 기업 우선지원)
  심사에서 "이미 개발 완료된 기술에 지원금 신청" 문제로 지적될 수 있는 리스크.
  KPI(정확도 95%↑ 등)에 현재 baseline을 같이 표기해서 "고도화 목표"임을
  명확히 하라고 제안.

**부수 발견**: 슬라이드 11(pgvector 200ms "구축 완료")과 슬라이드 13(같은 200ms를
"R&D 완료 시점 목표"로 재제시)이 서로 모순. "1,700+건 정제 포스터 DB"는 실제
DB(1,790건: published 437 + closed 1327 + review 20 + rejected 6)로 검증되어
정확함.

**커밋 없음** — 검토만, 코드/DB 변경 없음.

---

## 2. 검토 후 "AI 기능 구현" 우선순위 논의

리뷰 이후 사용자가 "여기서 말한 AI기능들을 구현하려면 어떻게 해야 할까?"라고
물어서, 신규 개발 대상(RAG 시맨틱 검색·스마트 공고 생성기·GenAI 포스터 생성·
AI 성과 리포팅)과 보완 대상(OCR 필드 구조화·파이프라인 정확도/속도·이미지
phash dedup)으로 나눠서 답변. 사용자가 **추천순으로 진행**을 요청해서, 아래
3건을 실제로 구현했다(전부 저위험·이미 있는 인프라 활용 가능한 순서로 추천함).

---

## 3. ✅ 이미지 phash 기반 중복판정 강화 (커밋 `ac1d153`)

**배경**: 2026-07-27에 이미 `image-phash.js`로 phash 계산·저장까지는 끝냈었는데
(`notice_sightings.image_phash`), 실제 중복판정 점수(`poster-duplicate-detector.js`)에는
반영이 안 돼 있었다 — 게시판과 블로그에 같은 사진이 서로 다른 URL로 올라와도
매칭이 안 되는 상태.

**구현**:
- `scorePosterDuplicate()`에 phash 해밍거리 기반 신호 추가(`arePhashSimilar`,
  기존 정확 URL 매칭(`imageMatched`)이 이미 잡았으면 중복 가산 안 함).
- `upload-to-supabase.js`: 이번 post의 phash를 한 번만 계산해서(다운로드 1회)
  중복판정과 `notice_sightings` 저장 양쪽에 재사용(`precomputedImagePhash`
  옵션 추가). 기존 후보(`loadDuplicateCandidates`/
  `loadNoticeCandidateDuplicateCandidates`)도 `notice_sightings`에서 phash를
  같이 읽어오도록 확장.
- `naver-blog-ingester.js`: 블로그 쪽에서 계산한 phash도 `linkOrCreateCandidate`의
  후보 지문에 포함.
- 유닛테스트 3건 추가(`poster-rules.test.js`) — 같은 사진·다른 URL 병합,
  정확 URL 매칭과 중복 가산 안 됨, 서로 다른 phash는 오탐 안 함. 55/55 전체
  테스트 통과.

---

## 4. ✅ OCR/원문 필드 구조화 확장 + 환각 검증 강화 (커밋 `481acd7`, `840905a`)

**배경**: `buildReadableNoticeInfo()`의 `facts`(기간/대상/내용/신청방법/장소/
문의처)는 이미 있었지만 **라벨 키워드("대상:", "문의처:" 등)를 찾는 정규식뿐**이라,
라벨 없는 문장체 텍스트나 OCR 노이즈에서는 필드가 많이 비어 있었다.

**구현**:
- `notice-facts-extractor.js` 신규 — 정규식이 6개 중 3개 미만을 채웠을 때만
  LLM으로 보완(대부분의 공고는 장소/문의처처럼 본문에 아예 없는 필드가 있는 게
  정상이라, "하나라도 비면 호출"이 아니라 "명백히 추출이 잘 안 된 경우"에만
  호출). 정규식 값은 절대 덮어쓰지 않음.
- **환각 방지**: 처음엔 LLM의 자가보고(`allFactsGroundedInText`)만 받았는데,
  가짜 응답을 몹업해서 테스트해보니 모델이 거짓으로 "true"라고 자가보고할 수
  있음을 확인 — 그래서 **원문과의 bigram 겹침 비율을 독립적으로 재검증**하는
  로직(`isGroundedInText`)을 추가해서, 자가보고와 무관하게 근거 없는 값은
  버리도록(null 유지) 고침. 몹 테스트로 실제 환각을 잡아내는 것까지 확인함.
- 실제 API로 테스트하다가 프롬프트 버그 하나 발견·수정: 처음엔 "장소" 필드에
  거주지 자격요건("마포구 거주")을 잘못 채워 넣었음(온라인 전용 프로그램인데
  물리적 장소가 없는데도 값을 채움) — target(자격요건)과 location(실제 장소)을
  명확히 구분하는 문구를 프롬프트에 추가해서 해결, 재테스트로 확인.
- `upsertNoticeCandidate`와 posters-insert 경로(실제로 저장되는 최종 지점)
  2곳에만 연결 — in-memory 중복후보 캐시용 호출(어차피 나중에 덮어써짐)에는
  안 걸어서 불필요한 LLM 비용을 피함.

---

## 5. ✅ 시맨틱 검색 적합도 스코어 노출 + 임베딩 백필 버그 발견/수정 (커밋 `1b4c81a`)

**배경**: `get_recommended_posters_v2`가 이미 찜/조회 이력 기반 관심 임베딩과
포스터 임베딩의 코사인 유사도를 계산해서 `recom_score`(지역+카테고리+대상+
마감+의미유사도 합산) 안에 녹여 쓰고 있었는데, 그 순수 유사도 값 자체는
한 번도 프론트에 노출된 적이 없었다.

**중요 발견**: 구현 확인차 실제 DB를 봤더니 **`posters.embedding`이 1,790건
전부 NULL** — 시맨틱 매칭 기능이 인프라(마이그레이션+RPC+HNSW 인덱스)는
있는데 실제로는 한 번도 작동한 적이 없었던 것.

**원인**: `backfill-poster-embeddings.js`가 다른 모든 크롤러 모듈이 쓰는 표준
`load-env.js` 대신 **자체적으로 만든, 틀린 dotenv 경로**(`apps/web/.env.local`,
`../.env`)를 쓰고 있었음 — `OPENAI_API_KEY`가 실제로 있는
`scripts/crawler/.env.local`은 아예 확인조차 안 하고 있었다. 그래서 매번
API 키를 못 찾아 조용히(fail-open) 아무것도 임베딩하지 않고 "skipped"로
끝났었다.

**조치**:
- `backfill-poster-embeddings.js`를 표준 `./load-env.js`로 교체.
- `get_recommended_posters_v2`에 `similarity_score`(순수 코사인 유사도
  0.0~1.0, 스케일 전 값) 컬럼 추가(마이그레이션은 `RETURNS TABLE` 컬럼이
  바뀌는 거라 `CREATE OR REPLACE`가 아니라 `DROP` 후 `CREATE` 필요).
- 백필 스크립트 실제 실행: **457/457건**(published 437 + review 20) 임베딩
  성공. 이후 재확인해보니 `recom_score`도 사용자별로 실제 편차가 생기기
  시작함(이전엔 임베딩이 없어서 전원 동일 점수였음) — **이 버그 수정으로
  개인화 추천 시스템 자체가 사실상 처음으로 실제 작동하기 시작함**(내가
  원래 하려던 "유사도 스코어 노출"보다 훨씬 큰 부수 효과).
- 프론트: `PosterCard`에 `similarityScore` prop 추가, 30% 이상일 때만
  "맞춤 NN%" 배지 노출(`page.tsx`의 `attachPosterMeta`에서 매핑). 로그인 안
  한 익명 방문자 경로는 이 필드 자체가 없어서 배지가 안 뜨는 것까지
  브라우저로 확인(콘솔 에러 없음). `tsc --noEmit` 통과.
- **로그인 상태에서 실제 배지가 뜨는 화면은 테스트 계정이 없어 브라우저로
  직접 확인은 못 함** — RPC 직접 호출로 값 검증, 타입체크, 로직 검토로
  대체함. 필요하면 다음에 실제 계정으로 확인 요망.

---

## 다음에 이어서 볼 만한 것

- Core: 지금은 "찜/조회 이력 기반 유사도"만 있고, 자연어 질의(자연어 검색창에
  입력 → 임베딩 → 매칭) 자체는 없음 — 사업계획서의 "RAG 기반 자연어 질의 이해"는
  이 신규 기능을 가리키는 것이므로 별도 구현 필요.
- Outbound(스마트 공고 생성기, GenAI 포스터 생성기, AI 성과 리포팅)는 전부
  손 안 댐 — 완전 신규 개발 항목이라 리소스/일정에 맞춰 별도 논의 필요.
- econ-gwangju(광주경제고용진흥원, gepa.or.kr) SSL 인증서 불일치는 여전히
  미해결 — 새 도메인 재조사 필요(2026-07-27 기록에서 이어짐).
